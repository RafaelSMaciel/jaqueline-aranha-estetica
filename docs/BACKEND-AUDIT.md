# Auditoria de Backend — Shiva Zen

Gerado por auditoria multi-agente (10 módulos). Total: **149** — 🔴 alta 31 · 🟡 média 71 · 🔵 baixa 47.

Status: marque [x] ao corrigir. Sobrescrever este arquivo conforme progride (nao versionar por data).

## 🔴 ALTA — corrigir primeiro (31)

- [ ] **[ARQUITETURA]** `aranha_estetica/models/profissionais.py` · Profissional.get_horarios_disponiveis (linhas 58-168)
  - **Problema:** Metodo de ~110 linhas dentro do model concentra logica de dominio (regras de agenda), persistencia (5+ querysets a Feriado/Excecao/Disponibilidade/Atendimento/Bloqueio), aritmetica de timezone (make_aware repetido) e geracao de slots em loop aninhado. Fat model classico: impossivel de testar isoladamente, alto risco de bug de slot, acoplamento a 5 outros models via _get_model/apps.get_model (anti-pattern para driblar import). Slot fixo de 30min (linha 135) ignora duracao real do procedimento ao gerar horarios — um procedimento de 90min ainda e oferecido em granularidade de 30min sem verificar se cabe na janela ate hora_fim_expediente.
  - **Fix:** Extrair para um service/disponibilidade.py (ex: SlotService.slots_livres(profissional, data, procedimento)). Manter no model apenas dados. Considerar granularidade de slot derivada de duracao_minutos+buffer e validar que o slot inteiro cabe antes de hora_fim_expediente.
- [ ] **[ARQUITETURA]** `aranha_estetica/services/agendamento.py + agendamento_service.py` · ambos definem `class AgendamentoService`
  - **Problema:** Existem DUAS classes com o mesmo nome AgendamentoService e responsabilidade sobreposta. agendamento.py (legado: criar/cancelar/reagendar com kwargs explicitos, valor_cobrado etc.) e exportado em services/__init__.py. agendamento_service.py (novo: criar(cmd) via Command DTO, consents, eventos) NAO e exportado e nunca aparece no __all__. Importar 'from services import AgendamentoService' pega o LEGADO; o novo so e alcancavel por path completo. Isso e armadilha de manutencao: dois fluxos de criacao de Atendimento divergentes coexistindo, fcil chamar o errado. A logica de checagem de slot esta triplicada (agendamento.py:36-41, agendamento_service.py:107 e _slot_ocupado:251-260, mais o QuerySet.conflito_com em models/agendamentos.py:38).
  - **Fix:** Consolidar numa unica classe/fonte de verdade. Reusar Atendimento.objects.conflito_com() (ja existe no manager) em vez de reimplementar o filtro 3x. Decidir qual API fica, deprecar a outra explicitamente (modulo legacy_*) e atualizar __init__.py/__all__ para refletir o service canonico.
- [ ] **[ARQUITETURA]** `aranha_estetica/utils/sms.py` · enviar_sms (linhas 133, 143) — retry com time.sleep
  - **Problema:** enviar_sms faz retry com time.sleep(2**tentativa) (ate 2+4=6s) de forma sincrona. enviar_otp_sms e chamado por OTPService.enviar_codigo dentro de solicitar_otp (services/otp.py:78), que e invocado diretamente pela view solicitar_otp_agendamento (views/booking_otp.py:53) no ciclo de request — nao em Celery. Em incidente do provedor, cada request OTP segura a thread/worker por ~6s, degradando o servidor e podendo estourar timeouts de gateway.
  - **Fix:** Mover envio de OTP/SMS para task Celery assincrona (ja existe send_email_async como padrao) ou, se precisar ser sincrono para feedback imediato, remover o time.sleep bloqueante (deixar o retry para um worker em background) ou reduzir drasticamente o backoff. Mesmo padrao aplica a whatsapp.py (linhas 104, 119).
- [ ] **[ARQUITETURA]** `aranha_estetica/domain/handlers.py` · consumir_sessao_pacote (l.40), disparar_workflow_nps (l.49), incrementar_contador_no_show (l.59), notificar_lista_espera (l.69)
  - **Problema:** Todos os handlers do EventBus sao stubs que apenas chamam logger.info — nenhum executa a logica que documentam. A logica real (debito de pacote, contagem de faltas, fila de espera) esta duplicada/implementada no signals.py. Isso e dead code arquitetural: cria uma camada de dominio que parece orientada a eventos mas nao faz nada, induzindo manutencao a achar que o debito de pacote acontece via evento quando na verdade ocorre no signal. Risco de alguem 'consertar' o handler e gerar debito duplo.
  - **Fix:** Decidir um caminho unico: ou migrar a logica do signal para os handlers (e o signal so publica eventos), ou remover a camada domain/ se nao usada. Enquanto coexistirem, documentar claramente que handlers sao no-op e que a fonte da verdade e o signal, para evitar debito/contagem dupla.
- [ ] **[ARQUITETURA]** `aranha_estetica/decorators_2fa.py` · staff_otp_required (linha 29) + interacao com middleware.py Enforce2FAMiddleware
  - **Problema:** Dois mecanismos de gating 2FA paralelos e divergentes. O middleware Enforce2FAMiddleware gateia /painel/ e /profissional/ usando request.session['otp_verified'] (flag custom setada por admin_2fa_verify) e reverse('aranha:admin_2fa_challenge'). O decorator staff_otp_required usa user.is_verified() (django-otp) e redireciona para 'two_factor:setup'/'two_factor:login' (rotas de outro pacote). Os criterios de 'verificado' sao diferentes (session flag vs is_verified()), entao um pode considerar o usuario verificado e o outro nao. Risco de bypass/loop e manutencao confusa.
  - **Fix:** Escolher UMA fonte de verdade para verificacao 2FA. Como o staff_otp_required nao e usado em nenhuma view (grep retorna 0 ocorrencias de uso), remove-lo e centralizar tudo no Enforce2FAMiddleware; ou, se preferir o decorator, alinhar criterio (is_verified) e rotas com o middleware.
- [ ] **[ARQUITETURA]** `C:/Users/rafae/Documents/shivazen-app/clinica/settings/__init__.py` · linhas 8-15 (selecao de _env)
  - **Problema:** A escolha entre dev e prod faz fallback para 'prod' SOMENTE se RAILWAY_ENVIRONMENT_NAME estiver setada, senao cai em 'dev' (DEBUG=True, cookies inseguros, SSL redirect off). railway.json usa builder NIXPACKS (nao o Dockerfile), e o DJANGO_ENV=prod so esta hardcoded no Dockerfile (linha ENV ...DJANGO_ENV=prod). No deploy NIXPACKS nao ha DJANGO_ENV=prod garantido; depende inteiramente de a Railway injetar RAILWAY_ENVIRONMENT_NAME. Se o nome dessa env var mudar/faltar, a app sobe em modo DEV em producao — vazamento de stack traces, DEBUG=True, SESSION/CSRF_COOKIE_SECURE=False.
  - **Fix:** Definir DJANGO_ENV=prod explicitamente nas variaveis de ambiente do servico Railway (e em railway.json/ variaveis), nao depender de RAILWAY_ENVIRONMENT_NAME. Alternativamente, inverter o default para 'prod' e exigir DJANGO_ENV=dev explicito em local (fail-safe: producao por padrao).
- [ ] **[CORRECAO]** `aranha_estetica/views/public.py` · lista_espera_publica, linhas 267-310
  - **Problema:** telefone vem cru de request.POST (apenas .strip(), sem normalizar_telefone) e e usado em Cliente.objects.get_or_create(telefone=telefone). O lookup compara o valor mascarado (ex '(17) 99999-0000') que nunca casa com a forma canonica armazenada ('17999990000'). get_or_create entao tenta CREATE; Cliente.save() normaliza o telefone e a constraint parcial unica uniq_cliente_telefone_ativo dispara IntegrityError nao tratado -> 500. Mesmo quando nao colide, cria cliente duplicado divergente da chave natural.
  - **Fix:** Normalizar antes do get_or_create: `from ..validators import normalizar_telefone; telefone = normalizar_telefone(request.POST.get('telefone',''))`, exatamente como confirmar_agendamento (booking_public.py:127-128) ja faz.
- [ ] **[CORRECAO]** `aranha_estetica/views/admin.py` · prontuario_consentimento, linha 38
  - **Problema:** annotate usa total_termos=Count('aceiteprivacidade', distinct=True), mas o model AceitePrivacidade foi removido na migration 0037 (DeleteModel) e unificado em AceiteTermo (related_name='aceites'). A relacao reversa 'aceiteprivacidade' nao existe mais, entao a query levanta FieldError e a pagina /painel/prontuario quebra com 500.
  - **Fix:** Trocar por Count('aceites', distinct=True) (related_name atual de AceiteTermo no Cliente) ou pela relacao correta. Adicionar teste de fumaca que renderiza essa view para pegar regressoes de schema.
- [ ] **[CORRECAO]** `aranha_estetica/views/admin.py` · admin_atualizar_status, linhas 130-133
  - **Problema:** Atualiza atendimento.status diretamente (atendimento.status = novo_status; atendimento.save()) sem usar o FSM _transicionar/TRANSICOES do model. Permite transicoes invalidas (ex.: REALIZADO -> PENDENTE, CANCELADO -> AGENDADO) que o model explicitamente proibe, corrompendo o estado e burlando regras/efeitos colaterais. save() sem update_fields tambem reescreve todas as colunas.
  - **Fix:** Usar os metodos de transicao do model (ou validar contra Atendimento.TRANSICOES) e retornar 400 quando a transicao for invalida. Passar update_fields=['status','atualizado_em'] ao salvar.
- [ ] **[CORRECAO]** `aranha_estetica/services/agendamento.py` · AgendamentoService.criar (linhas 36-55)
  - **Problema:** Race condition de check-then-insert: faz `.exists()` (l.36-41) e depois `.create()` (l.45). Entre os dois, outra requisicao pode inserir o mesmo slot (phantom read). O banco tem a EXCLUDE constraint excl_atendimento_sobreposicao (migration 0035) que dispara IntegrityError 23P01 na corrida perdida, MAS este criar() nao captura IntegrityError — entao em vez do ValueError amigavel 'Horario ja ocupado', a corrida vira um 500 nao tratado. As views booking_public.py:354 e booking_reagendar.py:186 tratam essa constraint, mas quem chamar o service direto nao recebe o erro de dominio.
  - **Fix:** Envolver o create() em try/except IntegrityError e, ao detectar a constraint de sobreposicao, re-levantar como ValueError('Horario ja ocupado...') — mesma traducao que as views ja fazem. Idealmente centralizar essa traducao no service para que TODOS os callers herdem o comportamento.
- [ ] **[CORRECAO]** `aranha_estetica/services/agendamento_service.py` · AgendamentoService.criar / _slot_ocupado (linhas 107-125, 251-260)
  - **Problema:** Mesmo defeito do legado: _slot_ocupado faz `.exists()` e depois Atendimento.objects.create() sem capturar IntegrityError da EXCLUDE constraint. Pior: aqui o create esta dentro de @transaction.atomic com side-effects ja persistidos antes (consents em _registrar_consents l.113, registrar_log nao). Na corrida perdida a IntegrityError aborta a transacao e sobe como 500 — o BusinessRuleViolation('Horario indisponivel') so cobre o caso detectado pelo check, nunca a corrida real.
  - **Fix:** Capturar IntegrityError no create() e re-levantar BusinessRuleViolation('Horario indisponivel.'), assim a corrida perdida vira erro de dominio consistente com o check-then-insert (e a transacao ja faz rollback dos consents).
- [ ] **[CORRECAO]** `aranha_estetica/services/fidelidade_service.py` · estornar_cashback_de_atendimento, loop l.122-161 (closure no on_commit)
  - **Problema:** Bug de late-binding de closure dentro do loop. O on_commit (l.152) captura `cred_mov` por referencia (so v e c sao bindados como default args). Quando os callbacks rodam apos o commit, `cred_mov` aponta para o ULTIMO item iterado — entao CashbackEstornado.movimento_estorno_id (l.155) recebe sempre o pk do ultimo credito, nao o de cada credito estornado. Com 2+ creditos no mesmo atendimento os eventos saem com id errado/duplicado. Secundario (l.147): no log 'credito_id' usa carteira.pk em vez do pk do credito (cred_mov.pk), rotulando errado o valor logado.
  - **Fix:** Bindar cred_mov como default arg na lambda igual a v e c: `lambda v=valor, c=carteira, m=cred_mov: ...` e usar m.pk em movimento_estorno_id. Corrigir o log para credito_id=cred_mov.pk.
- [ ] **[CORRECAO]** `aranha_estetica/tasks.py` · job_pesquisa_satisfacao_24h linhas 102-113
  - **Problema:** A Notificacao NPS e criada com status='PENDENTE' ANTES de enviar_nps_whatsapp(). Se o envio falhar (return False ou excecao), a Notificacao orfa permanece. Como o queryset usa .exclude(notificacao__tipo='NPS'), na proxima execucao o atendimento ja fica excluido pela notificacao orfa e o cliente NUNCA recebe o NPS (envio nunca e re-tentado). Alem disso, se o except amplo da task estourar e disparar self.retry, parte dos atendimentos ja terao Notificacao criada e nao serao reprocessados.
  - **Fix:** Criar a Notificacao com status='ENVIADO' somente apos sucesso (ou atualizar status para FALHOU e nao excluir por notificacao FALHOU). Ajustar o filtro para excluir apenas notificacoes NPS com status ENVIADO, permitindo re-tentativa de falhas.
- [ ] **[CORRECAO]** `C:/Users/rafae/Documents/shivazen-app/Procfile` · linha 'beat: celery -A clinica beat ... django_celery_beat.schedulers:DatabaseScheduler'
  - **Problema:** O processo beat usa o scheduler django_celery_beat.schedulers:DatabaseScheduler, mas 'django_celery_beat' NAO esta em INSTALLED_APPS (clinica/settings/base.py L53-71) nem em requirements.txt. O processo beat falha no boot (ModuleNotFoundError / app nao registrado e sem tabelas de schedule). Alem disso, CELERY_BEAT_SCHEDULE (base.py L419-452) so funciona com o PersistentScheduler/default, nao com o DatabaseScheduler vazio. Resultado: jobs cron (lembretes, NPS, expiracao de pacotes, purga LGPD) nunca rodam se o beat for iniciado assim.
  - **Fix:** Decidir uma abordagem: (a) usar scheduler padrao 'celery.beat.PersistentScheduler' no Procfile (le CELERY_BEAT_SCHEDULE do settings), OU (b) adicionar django-celery-beat ao requirements.txt e 'django_celery_beat' a INSTALLED_APPS e migrar o schedule para o admin. Garantir consistencia entre Procfile e settings.
- [ ] **[CORRECAO / SEGURANCA (webhook)]** `aranha_estetica/views/whatsapp.py` · whatsapp_webhook, linhas 121-124 (@require_http_methods(["POST"]))
  - **Problema:** O webhook do WhatsApp aceita SOMENTE POST. A Meta Business/Cloud API exige um handshake de verificacao via GET com hub.mode/hub.verify_token/hub.challenge que deve retornar o challenge em texto puro. Sem o ramo GET, a configuracao/assinatura do webhook na Meta falha (o endpoint retorna 405), e nao ha verificacao do verify_token. Resultado: integracao de respostas (confirmacao/cancelamento/NPS) nunca chega a funcionar em producao com a Meta.
  - **Fix:** Adicionar GET ao require_http_methods e tratar a verificacao: se request.method=='GET' e request.GET.get('hub.verify_token')==WHATSAPP_VERIFY_TOKEN, retornar HttpResponse(request.GET.get('hub.challenge')). Validar o verify_token com hmac.compare_digest. (Se o provider real for Twilio/Zenvia e nao Meta Cloud, o _verify_signature por X-Hub-Signature-256 nao bate com o formato deles — alinhar o esquema de assinatura ao provider efetivamente usado.)
- [ ] **[DADOS/QUERY]** `aranha_estetica/models/clientes.py` · Cliente — ausencia de SoftDeleteMixin; delete() default herdado de models.Model
  - **Problema:** Cliente implementa soft-delete a mao (campo deletado_em + ClienteAtivosManager + soft_delete()), mas NAO sobrescreve delete(). Logo Cliente.delete() ou QuerySet.delete() faz HARD delete fisico. Como Atendimento/AceiteTermo apontam para Cliente com on_delete=RESTRICT/PROTECT, um delete() acidental ou via admin levanta IntegrityError em vez de soft-delete — e em tabelas filhas com CASCADE (Prontuario, ListaEspera, ConsumoSessao via CompraPacote) apagaria dados clinicos/financeiros de verdade. O mixin SoftDeleteMixin existe e resolve isso, mas Cliente nao o usa, criando duas convencoes divergentes no mesmo codebase.
  - **Fix:** Fazer Cliente herdar de SoftDeleteMixin (ou ao menos sobrescrever delete() para chamar soft_delete()). Unificar a convencao: ou todos usam o mixin, ou documentar claramente por que Cliente diverge. Auditar callsites de Cliente.delete().
- [ ] **[DADOS/QUERY]** `aranha_estetica/services/comissao_service.py` · calcular_comissao (l.79-92) vs estornar_comissao (l.113-119) + handler _on_realizado (l.128-136)
  - **Problema:** Double-pay apos estorno-e-novo-Realizado. A idempotencia depende da UNIQUE constraint uniq_comissao_ativa_por_atendimento que SO cobre status IN (PENDENTE,PAGA) (models/extras.py:190-194). Quando uma comissao e estornada (status->ESTORNADA), a constraint deixa de bloquear. Se o AtendimentoRealizado for republicado para o mesmo atendimento (ex.: atendimento cancelado->reaberto->realizado de novo, ou replay de evento), calcular_comissao cria uma SEGUNDA MovimentoComissao valida — comissao paga em dobro. O event bus e best-effort e nao tem dedupe; nada garante 'um evento Realizado por atendimento'.
  - **Fix:** Antes de criar, checar se ja existe QUALQUER MovimentoComissao para o atendimento (incl. ESTORNADA) e decidir a regra de negocio (reativar a estornada vs. nao recriar). Ou tornar a UNIQUE constraint por atendimento independente de status (1 movimento por atendimento) e usar transicao de status para re-emissao controlada.
- [ ] **[DADOS/QUERY]** `aranha_estetica/utils/sms.py` · pode_enviar (linhas 62-81)
  - **Problema:** Rate-limit de SMS/OTP usa padrao read-modify-write nao-atomico: cache.get(key) seguido de cache.set(key, valor+1). Em Redis (prod) duas requisicoes OTP concorrentes do mesmo telefone/IP podem ambas ler o mesmo valor antes do incremento, ultrapassando o limite (SMS_MAX_POR_HORA=3). Como esta e a unica defesa anti-abuso/anti-custo do canal SMS pago (Zenvia), e explorVel para flood de OTP.
  - **Fix:** Usar operacao atomica: cache.incr(key) apos garantir a chave com cache.add(key, 0, timeout=3600), ou cache.add()+incr(); em django-redis incr e atomico. Checar o limite com o valor retornado pelo incr (e nao por get separado). Idealmente envolver tel/global/ip numa unica avaliacao atomica.
- [ ] **[DADOS/QUERY]** `aranha_estetica/signals.py` · processar_mudanca_status linhas 78-113 (bloco REALIZADO / debito de pacote)
  - **Problema:** Debito de sessao de pacote roda no signal sem transaction.atomic e sem select_for_update. O check-then-act `if sessoes_ja_feitas < item.quantidade_sessoes:` seguido de ConsumoSessao.objects.create() e um TOCTOU classico: dois atendimentos do mesmo cliente/pacote marcados REALIZADO concorrentemente leem a mesma contagem e ambos criam consumo, estourando o limite de sessoes do pacote. A UniqueConstraint(atendimento) so impede dois consumos do MESMO atendimento, nao protege o limite agregado do pacote.
  - **Fix:** Envolver o bloco em with transaction.atomic(): e usar CompraPacote.objects.select_for_update().filter(status='ATIVO', cliente=...) para serializar o debito por pacote; recontar sessoes dentro do lock antes do create. Idealmente mover para um service (ex: PacoteService.debitar_sessao) chamado explicitamente, nao num signal.
- [ ] **[DADOS/QUERY]** `aranha_estetica/tasks.py` · job_pesquisa_satisfacao_24h linhas 85-92
  - **Problema:** .exclude(notificacao__tipo='NPS') faz join na FK reversa Notificacao->Atendimento; combinado com o filtro positivo e select_related, o queryset pode multiplicar/duplicar linhas (mesmo atendimento com varias notificacoes) e o .count() na linha 94 nao reflete linhas distintas. Iterar sobre ele pode processar o mesmo atendimento mais de uma vez numa borda.
  - **Fix:** Usar ~Q(...) com subquery/Exists() ou adicionar .distinct(). Preferir Subquery/Exists para 'sem notificacao NPS' em vez de exclude com join.
- [ ] **[DADOS/QUERY]** `aranha_estetica/tasks.py` · job_limpeza_status_atendimentos linhas 437-440
  - **Problema:** Loop que faz atendimento.save() (save completo) por registro para mudar status para FALTOU. Cada save() dispara pre_save+post_save (capturar_status_anterior faz 1 query extra por item -> N+1) e re-executa o save() do model que regenera token_cancelamento. Em volume isso e O(N) saves e queries desnecessarias. Tambem nao usa as transicoes validadas (_transicionar) nem registra faltas via a regra de no-show.
  - **Fix:** Considerar bulk update controlado ou iterar com update_fields=['status'] e, se a contagem de faltas/efeitos colaterais e necessaria, chamar o metodo de transicao. No minimo usar .only()/iterator() e evitar regenerar token a cada save.
- [ ] **[DADOS/QUERY (transacao ausente)]** `aranha_estetica/views/pacotes.py` · admin_criar_pacote (53-71) e admin_editar_pacote (90-108)
  - **Problema:** Criacao do Pacote + N inserts de ItemPacote num loop sem transaction.atomic. Se um proc_id/qtd invalido fizer int() levantar ValueError no meio do loop, o Pacote (e itens parciais) ja persistidos ficam orfaos/incompletos, e o usuario ve 'Erro ao criar pacote' achando que nada foi salvo. Em admin_editar_pacote e pior: a linha 99 ja deletou TODOS os ItemPacote antes de recriar; uma falha no loop deixa o pacote sem nenhum item (perda de dados de configuracao).
  - **Fix:** Envolver o bloco create/edit + loop de itens em `with transaction.atomic():` para garantir all-or-nothing. Validar/converter proc_id e qtd com try local antes de criar, pulando linhas invalidas em vez de abortar.
- [ ] **[ERRO/LOG]** `aranha_estetica/utils/sms.py` · enviar_otp_sms (linhas 152-160) + pode_enviar (linhas 74-78)
  - **Problema:** pode_enviar() ja incrementa os contadores de quota ANTES do envio efetivo. Se enviar_sms() falhar (Zenvia retorna 4xx, timeout esgotado, ZENVIA_FROM ausente, etc.) a quota do cliente foi consumida mesmo sem nenhum SMS entregue. Um cliente legitimo pode ficar bloqueado (aguarde) por falhas do provedor, e o atacante consegue queimar a quota global de outros telefones.
  - **Fix:** Separar 'checar limite' de 'registrar consumo': so incrementar os contadores apos enviar_sms() retornar True. Ou decrementar/reverter o contador no caminho de falha. Tratar dev_only (que retorna True sem chamar API) de forma a nao distorcer a contagem real.
- [ ] **[ERRO/LOG (idempotencia/Celery)]** `aranha_estetica/views/cron.py` · run_job, linha 41 (job.apply())
  - **Problema:** O endpoint de cron externo executa as tasks Celery de forma SINCRONA via job.apply() dentro do request HTTP. Jobs como job_lgpd_purgar_inativos, job_aniversario_clientes e job_enviar_lembrete_dia_seguinte percorrem o banco e disparam e-mails/SMS; rodando inline eles bloqueiam o worker web e podem estourar o timeout do proxy (Railway), deixando o job em estado indeterminado. Alem disso nao ha protecao de idempotencia: se o cron externo re-tentar apos timeout, o job roda 2x (e-mails/SMS duplicados).
  - **Fix:** Enfileirar com job.delay() (ou apply_async) e retornar 202 com o task id imediatamente; ou, se a execucao sincrona for intencional para o free-tier sem worker, envolver cada job em um guard de idempotencia (ex.: lock por cache.add(f'cron:{job_name}:{date}') com TTL) para impedir reexecucao concorrente/duplicada, e documentar o limite de tempo.
- [ ] **[SEGURANCA]** `aranha_estetica/views/booking_api.py` · verificar_telefone, action 'enviar', linhas 219-235
  - **Problema:** Ao enviar OTP por SMS o codigo chama CodigoOtp.gerar_sms()+OTPService.enviar_codigo() diretamente, SEM verificar CodigoOtp.pode_reenviar(). A unica protecao e o ratelimit IP 5/m. Diferente de otp_service.solicitar_otp (que respeita REENVIO_MINIMO_SEG=60s). Um atacante com IPs rotativos, ou simplesmente 5 req/min, dispara 5 SMS pagos/min por telefone cadastrado — abuso de custo (Zenvia) e SMS-bombing da vitima.
  - **Fix:** Antes de gerar/enviar, checar CodigoOtp.pode_reenviar(CodigoOtp.email_para_telefone(telefone), proposito=PROPOSITO_LOGIN) e retornar 429/aguarde se falhar; idealmente reusar otp_service.solicitar_otp para centralizar a regra de reenvio.
- [ ] **[SEGURANCA]** `aranha_estetica/views/booking_public.py` · confirmar_agendamento, gate OTP linhas 142-154 + get_or_create por telefone linha 213
  - **Problema:** O 2FA (OTP) so e exigido quando o cliente e identificado por EMAIL ja cadastrado (cliente_existente = filter(email__iexact=...)). A identidade real do booking, porem, e o telefone (get_or_create(telefone=...) na linha 213). Um visitante que informe o telefone de outra pessoa (sem email, ou com email novo) NAO passa por OTP: cria/atualiza o Cliente alheio (sobrescreve nome linha 219-221, preenche data_nascimento/email vazios e flags de consentimento LGPD), e gera atendimentos PENDENTE em nome da vitima. Spoofing/poluicao de cadastro e consentimento.
  - **Fix:** Exigir OTP tambem quando Cliente.objects.filter(telefone=telefone, ativo=True).exists() (validar otp_agendamento contra o telefone/pseudo-email sms+<digitos>@shivazen.local). Para cliente novo sem cadastro, manter fluxo leve.
- [ ] **[SEGURANCA]** `aranha_estetica/views/admin_2fa.py` · admin_2fa_verify, linhas 77-94
  - **Problema:** O endpoint que valida o codigo TOTP pos-login (device.verify_token) NAO tem @ratelimit nem qualquer lockout. O django-axes (AXES_FAILURE_LIMIT) so intercepta o fluxo de login do Django auth, nao este POST custom. Um atacante com sessao parcialmente autenticada pode forca-bruta o 2FA (10^6 combinacoes) sem limite.
  - **Fix:** Aplicar @ratelimit(key='user', rate='5/m', method='POST', block=True) em admin_2fa_verify (e idealmente lockout apos N tentativas, registrando em audit). Mesma protecao deve cobrir o passo 'confirmar' no admin_2fa_setup.
- [ ] **[SEGURANCA]** `aranha_estetica/views/admin_usuarios.py` · _enviar_email_boas_vindas, linhas 175-190 (chamada em admin_criar_usuario:87)
  - **Problema:** A senha inicial e enviada em TEXTO PLANO por e-mail ('Senha inicial: {senha}'). E-mail nao e canal seguro e a credencial fica persistida na caixa do destinatario indefinidamente. Alem disso o envio usa fail_silently=True e except Exception: pass, entao se o e-mail falhar o admin acredita que a senha foi entregue (linha 91 sempre diz 'enviada por email') quando pode nao ter sido.
  - **Fix:** Nao enviar senha por e-mail: criar usuario sem senha utilizavel e mandar link de definicao de senha (mesmo fluxo de default_token_generator ja usado em admin_resetar_senha_usuario). Remover fail_silently=True/except pass para nao mascarar falha de entrega, ou ajustar a mensagem ao usuario conforme o resultado real do envio.
- [ ] **[SEGURANCA]** `aranha_estetica/views/admin_2fa.py` · admin_2fa_verify (linhas 82-91) e admin_2fa_challenge (linha 100-103)
  - **Problema:** Open redirect autenticado: next_url vem direto de request.POST.get('next') / request.GET.get('next') e e passado para redirect() sem validacao. Um atacante pode montar POST/link para /painel/seguranca/2fa/verify com next=https://evil.com e, apos a vitima inserir o token 2FA, ela e redirecionada ao dominio externo (phishing pos-login). O fluxo do Enforce2FAMiddleware so injeta next=request.path (server-side), mas a view aceita qualquer valor.
  - **Fix:** Validar com django.utils.http.url_has_allowed_host_and_scheme(next_url, allowed_hosts={request.get_host()}, require_https=request.is_secure()) e cair para 'aranha:painel_overview' quando invalido. Aplicar nas tres ocorrencias (verify ok-path, verify token-ok, challenge sem device).
- [ ] **[tests-coverage / lacuna de cobertura (modulo sem teste)]** `aranha_estetica/views/booking_reagendar.py` · modulo inteiro — grep 'reagendar' em tests/ retorna vazio
  - **Problema:** Reagendamento publico nao tem nenhum teste. Edge cases criticos sem cobertura: reagendar para horario em conflito, reagendar atendimento ja REALIZADO/CANCELADO, validade/expiracao do token de reagendamento, autorizacao por token (IDOR — token de um atendimento reagendando outro), reagendar para data passada. E um fluxo que escreve no banco e mexe em disponibilidade, alto risco de race/IDOR.
  - **Fix:** Criar test_reagendar.py cobrindo: token invalido->404, token de atendimento A nao reagenda B, conflito de horario rejeitado, atendimento ja finalizado nao reagenda, data passada rejeitada, e caminho feliz movendo data_hora_inicio/fim corretamente.
- [ ] **[tests-coverage / risco sem teste (pagamento/comissao)]** `aranha_estetica/tests/test_retorno_cashback_comissao.py` · ComissaoServiceTests (linhas 129-166) + ausencia total de teste para admin_financeiro.py
  - **Problema:** A transicao financeira de comissao PENDENTE->PAGA (payout / fechamento de periodo) nunca e testada. comissao_service.py define STATUS_PENDENTE/PAGA/ESTORNADA, mas os testes so cobrem criacao em PENDENTE (test_calcula_comissao_percentual) e estorno. O modulo que efetivamente paga/fecha comissao (views/admin_financeiro.py) tem ZERO referencias em qualquer test_*.py (grep 'financeiro|fechamento|comissao.*pag' = vazio). Bug no fechamento (pagar duas vezes, pagar comissao estornada, total errado) passaria despercebido — area de perda de dinheiro real.
  - **Fix:** Adicionar test_admin_financeiro.py: (a) staff marca movimentos PENDENTE como PAGA e idempotencia (re-disparar nao paga 2x); (b) comissao ESTORNADA nunca entra no total a pagar; (c) authz: anonimo/usuario comum nao acessa fechamento; (d) somatorio do periodo com Decimal correto. Exercitar via a view real, nao chamando .update() direto.

## 🟡 MÉDIA (71)

- [ ] **[ARQUITETURA]** `aranha_estetica/models/extras.py` · Carteira / MovimentoCarteira (linhas 23-92)
  - **Problema:** O docstring da Carteira afirma 'toda mutacao de saldo exige select_for_update() + transaction.atomic', mas o model NAO oferece nenhum metodo que garanta isso (ex: creditar/debitar). A regra fica delegada implicitamente a quem chama, e MovimentoCarteira.saldo_resultante e um campo livre que pode divergir do saldo real se gravado fora de um lock. O CheckConstraint saldo>=0 protege contra negativo, mas nao contra saldo_resultante inconsistente nem lost-update de saldo. Logica de negocio critica (dinheiro) sem encapsulamento no model/service.
  - **Fix:** Expor metodos transacionais no model ou service (Carteira.creditar(valor, origem, usuario)/debitar(...)) que facam select_for_update, recalculem saldo_resultante a partir do saldo travado e gravem Carteira+MovimentoCarteira na mesma transacao. Tornar saldo_resultante derivado, nunca passado pelo chamador.
- [ ] **[ARQUITETURA]** `aranha_estetica/views/booking_public.py` · confirmar_agendamento, linhas 108-382 (fat view ~270 linhas)
  - **Problema:** View concentra parsing, validacao de idade, slot lock, upsert de Cliente com 6 blocos de consentimento LGPD repetidos (linhas 194-244), criacao de Atendimento, parse de anamnese JSON, geracao de termos+Notificacao e disparo de 2 emails Celery. Logica de negocio (regra de conflito de horario, upsert de consentimento, criacao de termos) deveria estar em services/agendamento_service.py (que ja existe). Dificil de testar e duplica regras com api_horarios_disponiveis e reagendar.
  - **Fix:** Extrair para um service: criar_agendamento_publico(dados) -> Atendimento, e um helper aplicar_consentimentos(cliente, flags, ip, agora). Reaproveitar a deteccao de conflito ja presente em booking_reagendar.
- [ ] **[ARQUITETURA]** `aranha_estetica/views/admin_management.py` · admin_bulk_agendamentos, linhas 570-652
  - **Problema:** Fat view: duplica a logica de aprovacao/rejeicao que ja existe no AgendamentoService (usado por admin_aprovar/rejeitar logo acima), seta status sem passar pelo FSM, e nao envolve o multi-write (save + audit + email) em transaction.atomic. Em caso de erro no meio do loop, parte fica processada e parte nao. O fallback de e-mail (except: chama enviar_*_email sincronamente) bloqueia o request HTTP dentro do loop.
  - **Fix:** Delegar cada item ao AgendamentoService.aprovar/rejeitar (reuso + FSM + audit consistente). Envolver o lote em transaction.atomic e mover 100% do envio de e-mail para a task Celery (sem fallback sincrono que bloqueia o request).
- [ ] **[ARQUITETURA (logica de negocio/PII na view + DRY)]** `aranha_estetica/api/views.py` · AtendimentoSerializer / AtendimentoViewSet
  - **Problema:** O AtendimentoSerializer expoe valor_cobrado e os FKs cliente/profissional sem mascaramento, e a action 'hoje' duplica a logica de paginacao (page or qs / get_paginated_response) que ja existe no list. Imports locais (from django.utils import timezone dentro do metodo) espalhados. ClienteViewSet mascara PII mas AtendimentoViewSet, que tambem retorna cliente_nome (nome completo) sem mascarar, vaza PII por outro caminho para qualquer staff — aceitavel se staff e confiavel, mas inconsistente com a politica do ClienteSerializer.
  - **Fix:** Mover import de timezone para o topo do modulo; extrair helper de paginacao; decidir politica unica de PII (se nome completo pode aparecer em atendimentos, documentar; senao aplicar mask). Baixa prioridade funcional, media de consistencia.
- [ ] **[CORRECAO]** `aranha_estetica/models/profissionais.py` · BloqueioAgenda.expandir_ocorrencias (linhas 248-253) e get_horarios_disponiveis (linha 250)
  - **Problema:** rrulestr monta DTSTART com sufixo 'Z' (UTC) usando strftime sobre self.data_hora_inicio que pode ser timezone-aware no fuso local (America/Sao_Paulo). Formatar um datetime aware em horario LOCAL e anexar 'Z' (=UTC) desloca a recorrencia em ~3h, gerando ocorrencias de bloqueio em horarios errados. Recorrencias semanais/mensais de bloqueio podem cair no dia/horario incorreto.
  - **Fix:** Converter para UTC antes de formatar: self.data_hora_inicio.astimezone(timezone.utc).strftime('%Y%m%dT%H%M%SZ'), ou usar DTSTART local sem Z com TZID. Adicionar teste cobrindo bloqueio recorrente em fuso != UTC.
- [ ] **[CORRECAO]** `aranha_estetica/models/pacotes.py` · CompraPacote — valor_pago e Pacote.preco_total sem CHECK de nao-negativo no proprio CompraPacote
  - **Problema:** CompraPacote.valor_pago (linha 61) nao tem nenhum CheckConstraint nem MinValueValidator — aceita valor negativo no banco. Pacote tem chk_pacote_preco_positivo mas o valor efetivamente pago na compra nao e validado. Da margem a registro financeiro com valor pago negativo.
  - **Fix:** Adicionar CheckConstraint(Q(valor_pago__gte=0), name='chk_compra_pacote_valor_pago') e/ou MinValueValidator(0) no field.
- [ ] **[CORRECAO]** `aranha_estetica/models/nps.py` · AvaliacaoNPS.nota (linha 7)
  - **Problema:** nota = SmallIntegerField sem choices e sem MinValueValidator/MaxValueValidator no nivel Python — so existe o CheckConstraint de DB (0-10). Forms/admin/DRF nao recebem feedback de validacao amigavel; um full_clean() nao pega o range antes do INSERT, so o banco rejeita com IntegrityError cru.
  - **Fix:** Adicionar validators=[MinValueValidator(0), MaxValueValidator(10)] ao field para validacao em camada Python alem do CHECK.
- [ ] **[CORRECAO]** `aranha_estetica/models/clientes.py` · Cliente._gerar_codigo_indicacao (linhas 130-138)
  - **Problema:** Geracao de codigo de indicacao verifica colisao com .exists() em loop (ok) mas a checagem-e-criacao nao e atomica: dois cadastros concorrentes podem gerar o mesmo candidato, passar no exists() e colidir no INSERT (codigo_indicacao tem unique=True), levantando IntegrityError nao tratado no save(). O fallback de 12 chars tambem nao re-verifica unicidade.
  - **Fix:** Tratar IntegrityError no save com retry, ou confiar na UNIQUE e capturar/retry no insert. Documentar que a probabilidade e baixa mas existe sob concorrencia.
- [ ] **[CORRECAO]** `aranha_estetica/views/booking_public.py` · confirmar_agendamento, linha 173 (e booking_reagendar.py:109)
  - **Problema:** data_hora = datetime.fromisoformat(datetime_str) sem timezone.make_aware. Com USE_TZ=True (base.py:243), se o cliente postar um datetime SEM offset (ex '2026-06-21T14:00:00'), data_hora fica naive. A comparacao posterior em reagendar (nova_data <= agora, agora aware) lanca TypeError 'can't compare offset-naive and offset-aware', que NAO e IntegrityError nem DatabaseError -> escapa dos except (booking_reagendar.py:185-203 / booking_public.py:353-382) e vira 500. No confirm, persistir naive gera RuntimeWarning e interpretacao na TZ default, podendo deslocar o horario salvo.
  - **Fix:** Apos parse: `if timezone.is_naive(data_hora): data_hora = timezone.make_aware(data_hora)`. O endpoint api_horarios_disponiveis ja faz isso (booking_api.py:83) — alinhar os handlers POST.
- [ ] **[CORRECAO]** `aranha_estetica/views/public.py` · promocoes, linhas 138-145
  - **Problema:** Apesar do select_related('procedimento'), o loop faz 1-2 queries Preco por promocao (N+1): Preco.objects.filter(...).first() dentro do for. Numero de promocoes ativas e pequeno hoje, mas o padrao escala mal e ja existe util preco_base_map para resolver em lote.
  - **Fix:** Coletar proc_ids das promos e usar utils.precos.preco_base_map(procedimentos) (ja usado em booking_public.py:53) para montar o mapa de precos numa unica query.
- [ ] **[CORRECAO]** `aranha_estetica/views/admin_management.py` · admin_criar_bloqueio:68-69; admin_calendar.py admin_calendar_mover:122-123 e admin_calendar_events:44-45
  - **Problema:** datetime.fromisoformat(...) produz datetime NAIVE, mas o projeto tem USE_TZ=True e os campos data_hora_inicio/fim sao tz-aware. Django emite RuntimeWarning e interpreta o valor no fuso default, deslocando horarios (ex.: bloqueio/agendamento gravado 3h fora). Mesma classe de bug em varios lugares que parseiam ISO de input do usuario.
  - **Fix:** Aplicar django.utils.timezone.make_aware(...) (ou usar django.utils.dateparse.parse_datetime + make_aware se naive) antes de salvar. Centralizar num helper de parsing tz-aware reutilizado pelas views de calendario/bloqueio.
- [ ] **[CORRECAO]** `aranha_estetica/views/dashboard.py` · painel_overview, calculo de taxa_ocupacao linhas 70-76
  - **Problema:** slots_semana soma os slots de cada registro DisponibilidadeProfissional UMA vez (como se fosse um dia), mas compara com realizados_semana (7 dias). O denominador representa capacidade diaria agregada, nao semanal, entao a taxa de ocupacao fica inflada/sem sentido. Usa ainda timedelta.seconds (ignora .days, embora aqui nao estoure 24h) em vez de total_seconds().
  - **Fix:** Definir capacidade semanal corretamente (multiplicar a janela diaria por quantos desses dias caem na semana de referencia) e usar total_seconds(). Idealmente extrair o calculo para um helper testavel.
- [ ] **[CORRECAO]** `aranha_estetica/views/admin_calendar.py` · admin_calendar_mover, linhas 127-141
  - **Problema:** O reagendamento via drag-drop seta data_hora_inicio/fim e salva diretamente (alem do datetime naive ja citado), sem checar conflito de agenda/bloqueio/folga nem passar pelo FSM. Permite sobrepor atendimentos ou move-los para dentro de um BloqueioAgenda/folga existente, criando double-booking silencioso.
  - **Fix:** Validar colisao contra outros atendimentos do profissional e contra BloqueioAgenda/ExcecaoDisponibilidade antes de salvar, retornando 409/400 quando houver conflito. Reusar a logica de disponibilidade ja existente nos services de agendamento.
- [ ] **[CORRECAO]** `aranha_estetica/services/lista_espera_service.py` · notificar_compativeis (l.69-80) + _enviar_wa l.92
  - **Problema:** Side-effect (WhatsApp) disparado DENTRO de @transaction.atomic e antes do commit. _enviar_wa_lista_espera (l.79) faz chamada HTTP externa enquanto a transacao ainda esta aberta — se o commit falhar depois, o cliente ja recebeu WhatsApp dizendo que reservou um slot que nao existe (token nunca persistido). Tambem segura locks/transacao aberta durante I/O de rede (lento). Diferente dos outros services do modulo que corretamente usam transaction.on_commit para side-effects.
  - **Fix:** Coletar os envios e dispara-los via transaction.on_commit (ou enfileirar Celery) apos o save dos tokens, para nao mandar WhatsApp de reserva que pode sofrer rollback e nao bloquear a transacao em I/O.
- [ ] **[CORRECAO]** `aranha_estetica/utils/whatsapp.py` · enviar_confirmacao_d1 (linhas 204, 236-244)
  - **Problema:** O token de confirmacao/cancelamento (secrets.token_urlsafe(32)) e usado tanto no link enviado quanto salvo em Notificacao.token, mas a criacao do Notificacao com token nao trata IntegrityError de colisao de unique nem confirma transacao atomica. Alem disso, se enviar_template_whatsapp falhar, a Notificacao e criada com status FALHOU mas o token ja foi 'gasto' no link que nunca chegou ao cliente — nao ha caminho de reenvio idempotente (cada chamada gera novo token).
  - **Fix:** Envolver geracao+persistencia em transaction.atomic e tratar reenvio de forma idempotente (reutilizar token existente do atendimento em vez de gerar novo a cada tentativa). Garantir unicidade do token no model com constraint e retry em colisao.
- [ ] **[CORRECAO]** `aranha_estetica/signals.py` · processar_mudanca_status linhas 73-75 (registro de falta) e 78-81 (reset)
  - **Problema:** instance.cliente.registrar_falta()/resetar_faltas() chamam .save() completo do Cliente dentro do post_save do Atendimento, sem update_fields e sem transacao junto com a mudanca de status. Se o save do Atendimento ja commitou e o save do Cliente falha, fica inconsistente (status mudou mas falta nao foi contada). Tambem registrar_falta nao e idempotente: reprocessar/reenviar o mesmo evento (ou um segundo save sem mudanca real) poderia incrementar de novo — a guarda depende so de status_atual != status_anterior em memoria.
  - **Fix:** Usar update_fields nos metodos do Cliente e garantir atomicidade (mesma transacao do save do Atendimento). Tornar a contagem idempotente vinculando-a ao atendimento (ex: registrar que ESTE atendimento ja contou falta) em vez de incrementar cegamente.
- [ ] **[CORRECAO]** `aranha_estetica/signals.py` · processar_mudanca_status linhas 54-60 (gcal) e 37-52 (push) no bloco created
  - **Problema:** Side effects de I/O externo (push notification, Google Calendar) sao executados sincronamente dentro do post_save, dentro da transacao do request. Sao best-effort com except amplo (bom que nao quebram), mas: (1) bloqueiam a request com chamada de rede; (2) gcal_disponivel()/push rodam antes do commit — se a transacao externa der rollback depois, o push/gcal ja foram disparados para um atendimento que nao existe.
  - **Fix:** Mover para transaction.on_commit(lambda: ...) e/ou enfileirar via Celery (.delay) para nao bloquear a request nem disparar antes do commit.
- [ ] **[CORRECAO]** `aranha_estetica/tasks.py` · job_notificar_fila_espera linha 203
  - **Problema:** timezone.datetime.fromisoformat(data_livre_str).date(): o data_livre_str vem de instance.data_hora_inicio.isoformat() (datetime com tz). fromisoformat preserva o offset, mas .date() e tomado sobre o horario possivelmente UTC, nao no fuso local da clinica. Um agendamento as 23h local pode cair no dia seguinte em UTC, gerando match de data errado na fila de espera. Tambem nao ha tratamento se a string for invalida (ValueError vira retry infinito).
  - **Fix:** Converter para timezone local antes de .date() (timezone.localtime) ou passar ja a date isoformatada. Validar/normalizar a entrada e, em ValueError, logar e retornar sem retry (erro permanente, nao transitorio).
- [ ] **[CORRECAO]** `aranha_estetica/tasks.py` · send_email_async linhas 454-469 + job_alerta_detrator_nps l.169-187
  - **Problema:** send_email_async usa autoretry_for=(Exception,) e levanta RuntimeError quando func retorna falsy — qualquer falha (incl. permanente, ex: endereco invalido) sera re-tentada 3x. Sem idempotencia: se o email de fato foi enviado mas a funcao retornou falsy por outro motivo, re-tenta e duplica. Em job_alerta_detrator, o except interno (l.186) engole a falha do send_mail por detrator e NAO marca alerta_enviado, mas tambem nao re-tenta — alerta de detrator pode se perder silenciosamente (so um logger.error).
  - **Fix:** Distinguir erros permanentes vs transitorios no autoretry (autoretry_for restrito a excecoes de rede/SMTP transitorias). Em job_alerta_detrator, ou propagar para retry, ou persistir tentativa para reprocessamento posterior em vez de so logar.
- [ ] **[CORRECAO]** `aranha_estetica/tasks.py` · job_enviar_lembrete_dia_seguinte linhas 38-42
  - **Problema:** Filtro status='AGENDADO' apenas: agendamentos em status CONFIRMADO (ja confirmados pelo cliente) ou PENDENTE nao recebem lembrete D-1, mesmo que clinicamente devessem. Dado o FSM (PENDENTE->AGENDADO->CONFIRMADO), restringir a 'AGENDADO' pode silenciosamente pular lembretes legitimos dependendo de como o fluxo seta status. Valor default de filtro possivelmente incorreto.
  - **Fix:** Confirmar regra de negocio; provavelmente status__in=['AGENDADO','CONFIRMADO','PENDENTE'] conforme quais estados ainda precisam de lembrete. Documentar a escolha.
- [ ] **[CORRECAO]** `aranha_estetica/validators.py` · validate_data_nascimento (linha 51) e validate_maior_idade (linha 60)
  - **Problema:** validate_data_nascimento usa (hoje.year - value.year) > 150 para idade maxima, sem ajustar mes/dia — calculo de idade impreciso (aceita/rejeita por ~1 ano na borda). validate_maior_idade faz o ajuste correto (com (mes,dia)), gerando inconsistencia entre os dois validadores no mesmo campo.
  - **Fix:** Padronizar o calculo de idade nos dois: idade = hoje.year - value.year - ((hoje.month, hoje.day) < (value.month, value.day)); comparar idade > 150 / idade < 18. Extrair helper _idade(value) reutilizavel.
- [ ] **[CORRECAO]** `C:/Users/rafae/Documents/shivazen-app/clinica/settings/base.py` · DATABASES dj_database_url L181-186
  - **Problema:** dj_database_url.config(default=DATABASE_URL, conn_max_age=600) passa o proprio DATABASE_URL como 'default', mas .config() tambem le DATABASE_URL do ambiente — redundante e confuso. Mais relevante: o ramo so executa se DATABASE_URL existir, entao o 'default' nunca e usado de fato. Nao ha SSL configurado (ssl_require) — em Railway/prod com Postgres gerenciado e recomendavel exigir SSL. Tambem nao ha CONN_HEALTH_CHECKS aqui (so adicionado em prod.py L31, o que esta ok, mas o CONN_MAX_AGE de 600 e setado duas vezes: aqui e em prod.py L32).
  - **Fix:** Simplificar para dj_database_url.config(conn_max_age=600, conn_health_checks=True, ssl_require=not DEBUG) e remover a redefinicao duplicada de CONN_MAX_AGE em prod.py L32. Considerar ssl_require em prod.
- [ ] **[CORRECAO (status guard confuso / dead branch)]** `aranha_estetica/views/profissional.py` · marcar_realizado, linha 107
  - **Problema:** O guard `if atendimento.status in ['PENDENTE','REALIZADO','CANCELADO','FALTOU','REAGENDADO']` lista 5 dos status para BLOQUEAR, de modo que so AGENDADO/CONFIRMADO podem virar REALIZADO. A mensagem 'Atendimento ja esta como X' fica incorreta para PENDENTE (nao esta 'ja realizado', e pendente — nao deveria nem ser marcavel via este botao). A lista negativa hardcoded e fragil: um novo status nos choices nao bloqueado por engano vira marcavel. Logica por exclusao e propensa a erro.
  - **Fix:** Inverter para allowlist explicita: `if atendimento.status not in ('AGENDADO','CONFIRMADO'): warning('Nao e possivel marcar realizado a partir de {status}')`. Mensagem condizente com o estado real.
- [ ] **[DADOS/QUERY]** `aranha_estetica/models/pacotes.py` · CompraPacote.verificar_finalizacao (linhas 87-95)
  - **Problema:** Loop sobre self.pacote.itens.all() e dentro do loop executa self.sessoes_realizadas.filter(...).count() por item — N queries (N+1) e usa .count() em vez de comparar agregado. Para pacotes com varios itens cada chamada bate no banco repetidamente. Alem disso, a transicao para FINALIZADO + self.save() nao esta dentro de transaction.atomic junto com a leitura das contagens: race condition se duas sessoes forem consumidas concorrentemente (dois processos leem sessoes_feitas e ambos podem nao finalizar / finalizar em estado inconsistente).
  - **Fix:** Fazer uma unica query agregada (values('atendimento__procedimento').annotate(c=Count('id'))) e comparar em memoria; envolver leitura+save em transaction.atomic com select_for_update na CompraPacote.
- [ ] **[DADOS/QUERY]** `aranha_estetica/models/clientes.py` · Cliente.registrar_falta / resetar_faltas / soft_delete (linhas 140-156)
  - **Problema:** registrar_falta faz read-modify-write de faltas_consecutivas (self.x += 1; save()) sem select_for_update nem F() expression nem transaction.atomic: dois no-shows processados em paralelo podem perder incremento (lost update), subcontando faltas e nao bloqueando cliente que deveria ser bloqueado. Todos os tres metodos chamam self.save() completo, re-disparando normalizacao de telefone/cpf e geracao de tokens a cada mutacao trivial (overhead + risco de efeito colateral inesperado em campos nao relacionados).
  - **Fix:** Usar F('faltas_consecutivas')+1 com update() atomico, ou select_for_update dentro de transaction.atomic. Nos metodos de mutacao pontual usar save(update_fields=[...]) para nao reprocessar o objeto inteiro.
- [ ] **[DADOS/QUERY]** `aranha_estetica/models/agendamentos.py` · Atendimento._transicionar (linhas 143-167)
  - **Problema:** A transicao de status (save) e o registro de LogAuditoria nao estao na mesma transaction.atomic. Se o save() do status persistir mas o create do log falhar (capturado e apenas logado), tudo bem; porem o _publish_event roda DEPOIS do _transicionar, fora de qualquer transacao — se o evento dispara efeito colateral que grava no banco e ele falha parcialmente, o estado fica inconsistente (status mudou, efeito colateral parcial). Nao ha garantia atomic entre mudanca de estado e seus efeitos.
  - **Fix:** Avaliar envolver _transicionar (mudanca de status + auditoria) em transaction.atomic; documentar que efeitos de _publish_event sao explicitamente assincronos/best-effort para que ninguem assuma atomicidade com a transicao.
- [ ] **[DADOS/QUERY]** `aranha_estetica/views/booking_public.py` · confirmar_agendamento, linhas 294-300
  - **Problema:** Bloco duplicado: assinados_ids.update(AceiteTermo.objects.filter(cliente=cliente).values_list('versao_termo_id', flat=True)) aparece duas vezes identico (linhas 295-297 e 298-300). E uma query redundante por request (dead code/copy-paste) — sem efeito util pois e um set.update do mesmo conjunto.
  - **Fix:** Remover o segundo bloco (linhas 298-300).
- [ ] **[DADOS/QUERY]** `aranha_estetica/views/booking_api.py` · api_horarios_disponiveis, loop while linhas 82-114; api_dias_disponiveis linhas 182-185
  - **Problema:** Para cada profissional e cada slot de 30min do dia, dispara 2 queries .exists() (Atendimento ocupado + BloqueioAgenda) dentro do while — O(profissionais x slots) round-trips. api_dias_disponiveis tambem itera profissionais com 1 query DisponibilidadeProfissional cada (N+1). Em jornada longa com varios profissionais sao dezenas/centenas de queries por chamada AJAX (chamada a cada selecao de data).
  - **Fix:** Pre-carregar todos Atendimento/BloqueioAgenda do dia (por profissional) em memoria uma vez e checar overlap em Python; e prefetch das DisponibilidadeProfissional dos profissionais de uma so vez (filter(profissional_id__in=...)).
- [ ] **[DADOS/QUERY]** `aranha_estetica/views/admin_termos_compliance.py` · admin_termos_compliance, loop linhas 26-48
  - **Problema:** Para cada VersaoTermo ativa o loop dispara 1-3 queries (AceiteTermo.count, Cliente.count e, para nao-LGPD, Atendimento distinct count). Com N versoes vira N*3 queries (N+1 classico) no carregamento da pagina de compliance.
  - **Fix:** Pre-agregar fora do loop: um values('versao_termo_id').annotate(Count) em AceiteTermo para todas as versoes de uma vez; cachear Cliente.objects.filter(ativo=True).count(); e um agregado unico de atendimentos distintos por procedimento. Montar o resumo a partir desses dicts.
- [ ] **[DADOS/QUERY]** `aranha_estetica/views/admin_usuarios.py` · admin_usuarios, busca linha 27
  - **Problema:** qs.filter(nome__icontains=busca) | qs.filter(email__icontains=busca) combina dois QuerySets via operador OR depois de ja terem .select_related e .order_by aplicados. E fragil (depende de comportamento de combinacao do ORM) e, ao ser combinado com o filtro de papel logo abaixo, pode reaplicar/duplicar joins de forma inesperada.
  - **Fix:** Usar um unico filtro com Q: qs.filter(Q(nome__icontains=busca) | Q(email__icontains=busca)) e so depois aplicar o filtro de papel.
- [ ] **[DADOS/QUERY]** `aranha_estetica/views/admin_professional.py` · profissional_editar, linhas 109-138 e profissional_cadastro:43-66
  - **Problema:** Edicao deleta todas as DisponibilidadeProfissional e Habilitacao do profissional e recria uma a uma em loop, sem transaction.atomic. Se ocorrer erro no meio (ex.: hora invalida, proc inexistente), a disponibilidade/habilitacao fica parcialmente apagada e nao reconstruida, corrompendo a agenda do profissional. Tambem ha N inserts em loop (get_or_create por procedimento).
  - **Fix:** Envolver o delete+recreate em transaction.atomic. Usar bulk_create para disponibilidades e habilitacoes em vez de criar uma a uma no loop.
- [ ] **[DADOS/QUERY]** `aranha_estetica/services/pacote.py` · debitar_sessao_por_atendimento (l.20-54)
  - **Problema:** Corrida de over-debit. O CompraPacote ativo NAO e travado com select_for_update; sessoes_ja_feitas e contado com .count() (l.39-42) e depois ConsumoSessao.create. Dois atendimentos concorrentes do mesmo cliente/procedimento podem ambos ler sessoes_ja_feitas < quantidade e ambos criar consumo, debitando alem do limite do pacote (nao ha UNIQUE que impeca). Tambem ha N+1/ineficiencia: itera pacotes_ativos e por pacote faz duas queries (itens.filter e sessoes_realizadas.filter().count()) — o prefetch de 'pacote__itens' e 'sessoes_realizadas' nao e aproveitado porque .filter() em cima de related descarta o prefetch.
  - **Fix:** Adicionar .select_for_update() na query de CompraPacote (ja esta em @transaction.atomic) para serializar o debito. Para evitar N+1, filtrar em Python sobre os objetos ja prefetchados (pc.pacote.itens.all() / pc.sessoes_realizadas.all()) em vez de novas .filter().count().
- [ ] **[DADOS/QUERY]** `aranha_estetica/services/agendamento_service.py` · _upsert_cliente (l.229-249)
  - **Problema:** Dois problemas. (1) Race/IntegrityError: filtra Cliente por email__iexact e, se nao achar, create() — mas existe UNIQUE parcial uniq_cliente_email_ativo (Lower(email), migration 0034). Duas requisicoes simultaneas com mesmo email passam pelo .first()=None e ambas tentam create -> IntegrityError nao tratado (500). (2) Mass-write: no ramo de cliente existente faz cliente.save() sem update_fields (l.241), reescrevendo TODAS as colunas do cliente — pode sobrescrever campos alterados concorrentemente (lost update) e gera escrita desnecessaria.
  - **Fix:** Usar Cliente.objects.get_or_create / update_or_create com tratamento de IntegrityError (retry/refetch), e no update de campos opcionais usar save(update_fields=['telefone','cpf']) somente quando realmente mudou.
- [ ] **[DADOS/QUERY]** `aranha_estetica/services/lgpd.py` · purgar_inativos (l.100-113)
  - **Problema:** Loop com N writes em transacoes separadas e sem atomicidade do batch: itera candidatos.iterator() e chama esquecer_cliente() (cada um em sua propria @transaction.atomic). Se o job morrer no meio, fica metade anonimizada sem registro de progresso/auditoria. Alem disso esquecer_cliente nao registra LogAuditoria do evento de retencao (so logger.info), e nao ha rate/limite por execucao — uma base grande anonimiza tudo de uma vez. purgar_inativos tambem nao tem @transaction nem checkpoint.
  - **Fix:** Registrar auditoria (LogAuditoria/AuditoriaService) de cada anonimizacao para trilha LGPD, e considerar processar em lotes com limite/paginacao e log de progresso para idempotencia/retomada do job.
- [ ] **[DADOS/QUERY]** `aranha_estetica/utils/cache.py` · cached.wrapper (linhas 28-32) e cache_get_or_set (linhas 49-52)
  - **Problema:** A condicao 'if cached_val is not None' impede o cache de armazenar valores legitimamente None/falsy. Mais relevante: nao ha protecao contra cache stampede — sob expiracao concorrente, varias threads recomputam fn() simultaneamente. Para branding_dict (exemplo na docstring) e tolervel, mas para factories caras (queries pesadas) pode causar pico de carga.
  - **Fix:** Usar um sentinel (ex.: _MISS = object()) para distinguir 'ausente' de 'None cacheado'. Para keys quentes, considerar cache.get_or_set do Django ou lock/single-flight.
- [ ] **[DADOS/QUERY]** `aranha_estetica/tasks.py` · job_verificar_pacotes_expirando linhas 251-257
  - **Problema:** N+1: para cada CompraPacote, itera pc.pacote.itens.all() e para cada item faz pc.sessoes_realizadas.filter(...).count() — uma query por item por pacote dentro de dois loops aninhados (dias x pacotes x itens). Sem prefetch_related de itens/sessoes.
  - **Fix:** prefetch_related('pacote__itens__procedimento', 'sessoes_realizadas__atendimento') e calcular sessoes restantes em memoria, ou anotar contagens via aggregate/annotate. Substituir .count() por agregacao unica.
- [ ] **[DADOS/QUERY]** `aranha_estetica/admin.py` · AtendimentoAdmin.acao_marcar_realizado/cancelado/faltou (linhas 261-286) e ClienteAdmin.acao_anonimizar_lgpd (164-171)
  - **Problema:** Operacoes multi-write em loop sem transacao atomica. As acoes iteram o queryset chamando at.save() / LgpdService.esquecer_cliente() um por um. Se falhar no meio, fica em estado parcial (alguns atualizados, outros nao). Em lotes grandes tambem e N writes individuais. Alem disso nao ha registrar_log de auditoria nessas mudancas de status (LogAuditoria existe no projeto).
  - **Fix:** Envolver cada acao em with transaction.atomic(). Onde a transicao de status nao dispara side-effects de model/signal, considerar queryset.update(...) (como ja e feito em acao_bloquear_online/resetar_faltas). Adicionar registrar_log por registro afetado para rastreabilidade.
- [ ] **[DADOS/QUERY (race condition / idempotencia)]** `aranha_estetica/views/notificacoes.py` · confirmar_presenca, linhas 29-48
  - **Problema:** Read-modify-write em notif/atendimento sem select_for_update nem update atomico. Duas requisicoes concorrentes (ex.: duplo clique ou retry) passam ambas pelo check `not ja_respondeu` e gravam status duas vezes. Sem transacao, atendimento.save() e notif.save() podem divergir (atendimento muda mas notif nao, se houver erro entre os dois saves).
  - **Fix:** Envolver em transaction.atomic() e travar a notif com select_for_update(); ou usar um UPDATE condicional (Notificacao.objects.filter(pk=..., resposta__isnull=True).update(...)) e so mexer no atendimento se a row foi efetivamente atualizada (rowcount==1).
- [ ] **[DADOS/QUERY (transacao + sem notificacao real)]** `aranha_estetica/views/pacotes.py` · admin_vender_pacote, linhas 133-141
  - **Problema:** data_expiracao usa 30 * validade_meses dias — aproximacao grosseira (12 meses = 360 dias, perde 5 dias/ano; meses tem 28-31 dias). Para validade contratual de pacote isto gera vencimento incorreto. Tambem valor_pago vem direto do POST como string sem validacao/Decimal (depende de coercao implicita no model).
  - **Fix:** Usar dateutil.relativedelta(months=pacote.validade_meses) para somar meses civis corretamente. Validar valor_pago como Decimal e tratar entrada invalida explicitamente em vez de cair no except generico.
- [ ] **[ERRO/LOG]** `aranha_estetica/models/agendamentos.py` · Atendimento._publish_event (linhas 199-213)
  - **Problema:** except Exception: pass silencia QUALQUER erro na publicacao de eventos de dominio (confirmacao/cancelamento/realizado). Se o event bus quebrar (erro de import, bug no handler sincrono, etc.) nenhum log e emitido — efeitos colaterais como notificacao/comissao/cashback podem deixar de disparar silenciosamente e ninguem percebe. Contrasta com _transicionar logo acima, que ao menos loga o warning de auditoria.
  - **Fix:** Capturar e logar: except Exception as exc: logging.getLogger(__name__).warning('event_publish_falhou', extra={'event': event_name, 'atendimento_id': self.pk, 'erro': str(exc)}). Manter best-effort mas com rastro.
- [ ] **[ERRO/LOG]** `aranha_estetica/views/booking_api.py` · cancelar_agendamento, linhas 296-298
  - **Problema:** except Exception as e amplo engolindo qualquer erro e retornando 500 generico. Mascara bugs reais (ex AttributeError) como 'erro interno'. Alem disso o cancelamento faz atendimento.save() sem transaction nem select_for_update e nao libera o slot lock/cache nem notifica profissional — duas requisicoes podem cancelar concorrentemente sem efeito de dados, mas o broad-except impede diagnostico.
  - **Fix:** Restringir para except DatabaseError (e deixar erros de programacao propagarem para o handler 500 do Django, que loga stack). Considerar transaction.atomic + select_for_update no atendimento ao mudar status.
- [ ] **[ERRO/LOG]** `aranha_estetica/views/admin_promotions.py` · admin_criar_promocao:39 e admin_editar_promocao:67 (try/except Exception)
  - **Problema:** get_object_or_404(Procedimento, ...) esta dentro de um try/except Exception generico. Http404 (procedimento inexistente/ID ausente) e capturado e transformado na mensagem generica 'Erro ao criar/atualizar promocao', escondendo a causa real (validacao de input) e tratando 404 como erro interno. Padrao de except Exception amplo se repete em quase todas as views de create/edit (procedimento, bloqueio, promocao, profissional) engolindo a stack util.
  - **Fix:** Validar a existencia do procedimento fora do try (ou capturar excecoes especificas: ValueError/IntegrityError) e dar mensagens distintas para input invalido vs erro interno. Evitar except Exception generico como tratamento padrao de formularios.
- [ ] **[ERRO/LOG]** `aranha_estetica/services/comissao_service.py` · handlers _on_realizado / _on_cancelado (l.128-145) e fidelidade_service.py:165-185, retorno_service.py:102-114, lista_espera_service.py:116-124
  - **Problema:** Idempotencia de eventos depende exclusivamente de constraints de banco; o EventBus.publish (domain/event_bus.py:54-77) e best-effort e engole excecoes de handler com log, sem retry nem dead-letter. Se AtendimentoRealizado for publicado e um handler (ex.: comissao) lancar antes do create, o evento e perdido silenciosamente — comissao/cashback/retorno nunca processam para aquele atendimento e nada re-tenta. Nao ha registro de 'evento processado' para reconciliacao.
  - **Fix:** Para os efeitos financeiros (comissao, cashback) considerar processamento via Celery com retry/idempotency-key em vez do bus sincrono best-effort, ou ao menos uma tabela de outbox/eventos processados para auditoria e reprocesso.
- [ ] **[ERRO/LOG]** `aranha_estetica/utils/sms.py` · pode_enviar (linhas 79-80)
  - **Problema:** except Exception: pass engole silenciosamente qualquer erro do backend de cache durante o incremento dos contadores de rate-limit. Se o Redis cair, a funcao retorna True (libera o envio) sem nenhum log, desativando silenciosamente toda a protecao anti-abuso de SMS exatamente no momento em que ela mais importa.
  - **Fix:** Capturar excecao especifica do cache e logar (logger.error('sms_rate_limit_cache_falha', ...)). Decidir explicitamente a politica de fail-open vs fail-closed; para um canal pago, considerar fail-closed (negar) ou ao menos alertar.
- [ ] **[ERRO/LOG]** `aranha_estetica/utils/audit.py` · registrar_log (linha 28: except Exception)
  - **Problema:** except Exception captura tudo e apenas loga em nivel warning, garantindo que nenhuma operacao principal quebre. A intencao e boa, mas captura ampla demais pode mascarar bugs de programacao (ex.: campo inexistente no model, detalhes nao-serializaveis em JSON) que passariam despercebidos, levando a perda silenciosa de trilhas de auditoria — relevante para LGPD/compliance.
  - **Fix:** Estreitar para (DatabaseError, TypeError, ValueError) ou ao menos logar com exc_info=True/stack para diagnostico. Considerar um contador/metric de falhas de audit para deteccao.
- [ ] **[ERRO/LOG]** `aranha_estetica/tasks.py` · job_enviar_lembrete_dia_seguinte (l.63), job_promocao_mensal (l.394), job_notificar_fila_espera (l.226), job_verificar_pacotes_expirando (l.272), job_aniversario_clientes (l.324)
  - **Problema:** except Exception amplo envolvendo o batch inteiro + self.retry. Se uma falha ocorre no meio do loop (ex: 1 email falha) e a excecao sobe, a task inteira e re-tentada do zero, re-enviando emails aos destinatarios ja processados (sem checkpoint de idempotencia em promocao/fila por destinatario alem do flag notificado). Resulta em e-mails duplicados em promocao/lembrete/aniversario.
  - **Fix:** Envolver o envio de cada destinatario em try/except interno (log + continue) e so deixar subir/retry erros realmente transitorios e globais (ex: broker). Para promocao/aniversario, idealmente marcar por-destinatario o envio (flag/registro) para retomada idempotente.
- [ ] **[ERRO/LOG]** `C:/Users/rafae/Documents/shivazen-app/clinica/settings/prod.py` · CELERY_TASK_ALWAYS_EAGER L39-41
  - **Problema:** Em prod sem REDIS_URL, Celery roda eager (sincrono) com CELERY_TASK_EAGER_PROPAGATES=False. Isso significa que tasks disparadas dentro de requests HTTP rodam inline (bloqueando a resposta) e, com PROPAGATES=False, exceptions nas tasks sao silenciadas (engolidas) — falhas de envio de e-mail/lembrete/NPS nao aparecem como erro no fluxo nem necessariamente em log. Combinado com o comentario 'Cron externo chama via HTTP', operacoes longas (jobs de purga LGPD, expiracao de pacotes) rodariam sincronas dentro de um request, sujeitas ao --timeout 120 do gunicorn.
  - **Fix:** Avaliar manter PROPAGATES=True em prod para nao engolir erros, ou garantir Redis+worker em prod (broker real). Se eager for mantido, garantir que cada task tenha try/except com logging interno e que jobs longos nao rodem dentro do ciclo de request HTTP.
- [ ] **[ERRO/LOG (except broad/silencioso)]** `aranha_estetica/views/pacotes.py` · admin_criar_pacote (75-77), admin_editar_pacote (112-114), admin_vender_pacote (149-151)
  - **Problema:** except Exception generico envolvendo todo o fluxo (incluindo get_object_or_404 e int()). Engole TypeError/ValueError de input invalido, IntegrityError de banco e Http404 (get_object_or_404 levanta Http404 que NAO e Exception subclass de forma util aqui — na verdade Http404 herda de Exception e seria capturada, transformando um 404 legitimo em 'Erro ao vender pacote'). Mensagem ao usuario e identica para qualquer causa, dificultando diagnostico.
  - **Fix:** Reduzir o escopo do try ao bloco de escrita; deixar get_object_or_404 fora (para 404 real). Capturar IntegrityError/ValidationError especificamente e dar mensagens distintas. Manter logger.error com exc_info (ja existe), mas nao mascarar 404.
- [ ] **[ERRO/LOG (except silencioso engole auditoria)]** `aranha_estetica/views/auth.py` · ClinicaPasswordResetView.form_valid (120-121) e ClinicaPasswordResetConfirmView.form_valid (153-154)
  - **Problema:** try/except Exception com `pass` ao redor da criacao de LogAuditoria. Falha em registrar a auditoria de reset/redefinicao de senha (evento sensivel de seguranca) e completamente silenciosa — nenhum log de erro. Em incidente de seguranca o rastro de auditoria pode simplesmente nao existir sem ninguem perceber.
  - **Fix:** Trocar `pass` por `logger.warning('falha ao registrar LogAuditoria de reset de senha', exc_info=True)` para que a falha de auditoria seja observavel, mantendo o fluxo de reset funcionando.
- [ ] **[PADRAO]** `aranha_estetica/services/agendamento.py` · criar (assinatura, l.28-29)
  - **Problema:** Type hints incorretos: valor_cobrado/valor_original anotados como Optional[float], mas o campo Atendimento.valor_cobrado e DecimalField (models/agendamentos.py:105). Passar float a um DecimalField e fonte classica de erro de precisao/quantize e mistura float+Decimal. O hint induz callers a passar float.
  - **Fix:** Trocar os hints para Optional[Decimal] e garantir que callers passem Decimal. Idem em qualquer doc/exemplo.
- [ ] **[PADRAO]** `aranha_estetica/utils/pii.py` · mask_email/mask_telefone/mask_cpf — duplicadas em security.py e precos.py
  - **Problema:** Existem TRES implementacoes de mask_telefone (pii.py:34 -> '17****0000'; security.py:28 -> '***-3210'; precos.py:42 -> '***0000') e DUAS de mask_email/mask_cpf (pii.py vs security.py) com formatos e niveis de ocultacao diferentes. serializers.py/auditoria.py importam de security.py, booking views de pii.py, whatsapp.py de precos.py. Resultado: mesmo telefone aparece mascarado de 3 jeitos nos logs, e qualquer ajuste de politica LGPD precisa ser feito em 3 lugares (risco de divergencia/regressao).
  - **Fix:** Consolidar em um unico modulo (pii.py e o mais documentado) e reexportar de security.py/precos.py por compatibilidade, ou trocar os imports. Remover as copias divergentes. Padronizar o formato de saida.
- [ ] **[PADRAO]** `aranha_estetica/decorators_2fa.py` · staff_otp_required (linha 29) — modulo inteiro
  - **Problema:** Dead code: staff_otp_required nao e importado nem usado em nenhuma view (verificado por grep em views/). Mantem uma segunda implementacao de 2FA que nunca executa, aumentando superficie de confusao/bug.
  - **Fix:** Remover decorators_2fa.py (ou o decorator) apos confirmar que nenhuma rota depende dele. Se for mantido como API futura, marcar claramente e cobrir por teste.
- [ ] **[PADRAO]** `aranha_estetica/forms/cliente.py` · DsarForm (linha 34)
  - **Problema:** Dead code: DsarForm (Data Subject Access Request) e definido mas nunca importado em forms/__init__.py nem usado em nenhum lugar (grep so encontra a definicao). Codigo morto relacionado a LGPD pode dar falsa sensacao de que o fluxo DSAR existe.
  - **Fix:** Remover DsarForm ou conecta-lo a uma view real de DSAR. Se LGPD DSAR for requisito, abrir tarefa.
- [ ] **[SEGURANCA]** `aranha_estetica/models/agendamentos.py` · Notificacao.__str__ (linhas 309-316) e Atendimento.__str__ (254-258)
  - **Problema:** __str__ embute nome do cliente (PII). __str__ e chamado em logs de admin/Django, mensagens de erro, repr em tracebacks e paginas de admin — vazando PII de cliente para logs e telas que podem ter audiencia mais ampla que o dado clinico merece. ListaEspera, CompraPacote, AvaliacaoNPS, AceiteTermo tem o mesmo padrao.
  - **Fix:** Para entidades sensiveis, preferir __str__ com identificador nao-PII (ex: f'Atendimento #{self.pk} {data_fmt}') ou nome parcial. No minimo garantir que logs estruturados nao serializem o objeto via str().
- [ ] **[SEGURANCA]** `aranha_estetica/views/admin_branding.py` · admin_branding, upload de logo linhas 72-90
  - **Problema:** Validacao do upload de logo so checa a extensao do nome do arquivo (.svg incluido) e o tamanho; nao valida content-type/magic bytes nem sanitiza SVG. SVG aceito e servido inline pode conter <script> (XSS armazenado) quando renderizado no painel/site. Extensao tambem e trivialmente falsificavel.
  - **Fix:** Validar o conteudo real (ex.: Pillow para raster) e, para SVG, ou bloquear ou sanitizar (remover script/eventos) e servir com Content-Disposition/CSP adequado. Confirmar via magic bytes, nao so pela extensao.
- [ ] **[SEGURANCA]** `aranha_estetica/views/admin_management.py` · termo_assinatura, linhas 389-438
  - **Problema:** Diferente de nps_web (que filtra tipo='NPS' e aplica NPS_TOKEN_EXPIRY), o termo_assinatura aceita get_object_or_404(Notificacao, token=token) sem filtrar por tipo e sem qualquer expiracao de token. Qualquer token de notificacao valido (de qualquer tipo, possivelmente antigo) abre a tela de assinatura de termos daquele cliente, ampliando a janela e a superficie de uso indevido do link.
  - **Fix:** Restringir o tipo de notificacao aceito e aplicar uma expiracao (analoga a NPS_TOKEN_EXPIRY). Considerar invalidar o token apos a assinatura.
- [ ] **[SEGURANCA]** `aranha_estetica/utils/security.py` · client_ip (linhas 45-50)
  - **Problema:** client_ip retorna o PRIMEIRO IP de X-Forwarded-For sem validar contra proxies confiaveis. Esse valor e usado como chave de rate-limit (sms_rl:ip:{ip}) e em audit logs. Como o cliente controla totalmente o header XFF, um atacante pode injetar IPs arbitrarios/falsos para (a) burlar o rate-limit por IP enviando IPs diferentes a cada request e (b) poluir/falsificar trilhas de auditoria de seguranca. Em deploy atras de 1 proxy confiavel, o IP correto e normalmente o ULTIMO da cadeia, nao o primeiro.
  - **Fix:** Confiar apenas no numero conhecido de proxies (pegar o IP a partir do fim da lista conforme a profundidade do proxy) ou usar uma lib como django-ipware com NUM_PROXIES configurado. Documentar o pressuposto de topologia de rede. Nunca usar o XFF cru como identidade de seguranca.
- [ ] **[SEGURANCA]** `aranha_estetica/middleware.py` · ContentSecurityPolicyMiddleware.ALLOWED_IMG_SRCS (linha 124-128) e style-src-attr (linha 170)
  - **Problema:** img-src inclui 'https:' generico, permitindo carregar imagem de QUALQUER origem HTTPS (vetor de exfiltracao/tracking e de bypass parcial de CSP via <img>). E style-src-attr 'unsafe-inline' permite style=... inline (XSS via injecao de style/expression em browsers antigos / data exfil via background-image). O proprio docstring reconhece como debito (Lote 3.5).
  - **Fix:** Restringir img-src a origens conhecidas (self, data:, dominio de CDN/storage usado). Planejar remocao de style-src-attr/script-src-attr 'unsafe-inline' migrando os ~645 style attrs / 34 handlers para CSS/listeners externos, ja rastreado como debito.
- [ ] **[SEGURANCA]** `aranha_estetica/decorators.py` · profissional_required._wrapped_view (linha 34)
  - **Problema:** Acesso ao atributo relacionado via getattr(user, 'profissional', None) dispara uma query por request e, se o usuario nao tiver profissional, levanta RelatedObjectDoesNotExist que getattr NAO captura (getattr so captura AttributeError). Em alguns setups de OneToOne o acesso lanca a excecao do descriptor; dependendo da versao pode propagar 500 em vez de redirecionar. Comportamento depende fragilmente do tipo de relacao.
  - **Fix:** Tratar explicitamente: usar try/except (ObjectDoesNotExist) ou checar via Profissional.objects.filter(usuario=user, ativo=True).exists(), evitando depender do comportamento de getattr sobre descriptor de relacao.
- [ ] **[SEGURANCA]** `aranha_estetica/forms/procedimento.py` · ProcedimentoForm.Meta.fields (linha 10)
  - **Problema:** fields = '__all__' (mass-assignment). Qualquer campo do model Procedimento fica editavel pelo form, incluindo campos que deveriam ser controlados (ex: slug, link_videoconferencia, imagem_destaque URLField sem validacao de dominio, modalidade). Em form admin custom ou view publica isso amplia superficie de input.
  - **Fix:** Listar explicitamente os fields permitidos em vez de '__all__'. Validar imagem_destaque/link_videoconferencia (URLField aceita qualquer URL — possivel SSRF/conteudo externo se renderizado).
- [ ] **[SEGURANCA]** `C:/Users/rafae/Documents/shivazen-app/clinica/settings/base.py` · ALLOWED_HOSTS L46-49 e CSRF_TRUSTED_ORIGINS L323-327
  - **Problema:** os.environ.get(...).split(',') nao faz strip de espacos. Se ALLOWED_HOSTS ou CSRF_TRUSTED_ORIGINS vier com espaco apos virgula (ex: 'a.com, b.com'), entram hosts com espaco que nunca casam (' b.com'), podendo causar 400 Bad Request silencioso em prod. Se a env var for setada como string vazia explicita (''), split('') retorna [''] — um host vazio invalido — em vez de cair no default.
  - **Fix:** Normalizar: [h.strip() for h in os.environ.get('ALLOWED_HOSTS','127.0.0.1,localhost').split(',') if h.strip()]. Mesmo tratamento para CSRF_TRUSTED_ORIGINS. Evita falhas dificeis de diagnosticar.
- [ ] **[SEGURANCA]** `C:/Users/rafae/Documents/shivazen-app/clinica/settings/base.py` · RATELIMIT_USE_CACHE / CACHES L316-317, L398-413
  - **Problema:** RATELIMIT_FAIL_OPEN=False (bom) e RATELIMIT_USE_CACHE='default'. Porem quando REDIS_URL nao esta definida (L409-412), o cache default vira LocMemCache, que e por-processo. Com gunicorn rodando multiplos workers (Procfile: --workers 4; railway.json: --workers 1; Dockerfile: WEB_CONCURRENCY default 2), o rate-limit/axes-cache nao e compartilhado entre workers — limites ficam multiplicados pelo numero de workers e o lockout do axes (que usa cache p/ alguns contadores) fica inconsistente. Em prod sem Redis isso enfraquece o brute-force protection.
  - **Fix:** Em prod exigir REDIS_URL (ou DB cache compartilhado) para o backend de rate-limit/axes; falhar o boot ou logar warning alto se prod sem Redis. Alternativamente usar AxesDatabaseBackend que persiste em DB. Documentar que LocMemCache nao deve ser usado em prod multi-worker.
- [ ] **[SEGURANCA (PII em log / hash instavel)]** `aranha_estetica/views/auth.py` · ClinicaPasswordResetView.form_valid, linhas 112-119
  - **Problema:** Dois problemas: (1) logger.info usa extra={'email_hash': hash(email)} — o built-in hash() do Python tem seed aleatorio por processo (PYTHONHASHSEED), entao o 'hash' nao e consistente entre processos/restarts e e inutil para correlacao; alem disso hash() de string curta nao oferece anonimizacao real. (2) O LogAuditoria.acao grava o email em texto puro (f'... email: {email}') — PII (e-mail) persistida em claro na tabela de auditoria, divergindo da politica anti-PII aplicada em outros pontos (ex.: login mascara o email).
  - **Fix:** Usar hashlib.sha256(email.encode()).hexdigest()[:12] para o log estruturado, e mascarar o email no campo acao do LogAuditoria (ou guarda-lo so no campo proprio mascarado), alinhando com o padrao de mask aplicado no usuario_login.
- [ ] **[SEGURANCA (endpoints de schema publicos)]** `aranha_estetica/api/urls.py` · linhas 21-23 (SpectacularAPIView / Swagger / Redoc)
  - **Problema:** Os endpoints de schema OpenAPI e UIs (swagger/redoc) sao registrados sem permission_classes. Como o REST_FRAMEWORK.DEFAULT_PERMISSION_CLASSES e IsAuthenticated, a SpectacularAPIView pode herda-lo, mas SpectacularSwaggerView/RedocView servem HTML e tipicamente ficam publicos por padrao — expondo a estrutura completa da API (rotas, params, modelos Cliente/Atendimento) a anonimos. Em uma clinica isso e reconhecimento facil para um atacante mapear a superficie.
  - **Fix:** Restringir schema/swagger/redoc a staff: definir SPECTACULAR_SETTINGS['SERVE_PERMISSIONS'] = ['rest_framework.permissions.IsAdminUser'] e/ou envolver as views com permissao de staff. Confirmar com teste anonimo que /api/schema/ retorna 403.
- [ ] **[SEGURANCA (rate-limit / CSRF surface)]** `aranha_estetica/views/notificacoes.py` · confirmar_presenca, linhas 19-24
  - **Problema:** Endpoint publico sem login que muda status do atendimento (CONFIRMADO/CANCELADO) via POST com token. Nao tem @require_http_methods nem rate-limit. Embora o token seja secrets.token_urlsafe(32) (forte), a ausencia de rate-limit permite brute-force/enumeracao de tokens em larga escala e abuso (varredura). Tambem aceita qualquer metodo HTTP (PUT/DELETE) sem restricao.
  - **Fix:** Adicionar @require_http_methods(['GET','POST']) e @ratelimit(key='ip', rate='30/m', block=True). Opcionalmente invalidar/expirar o token apos resposta (ja ha guard ja_respondeu, mas nao impede tentativas repetidas de tokens diferentes).
- [ ] **[SEGURANCA (rate-limit redundante/inconsistente)]** `aranha_estetica/views/auth.py` · usuario_login, _check_rate_limit (21-32) + decorators (35-36) + chamada (46)
  - **Problema:** Coexistem dois mecanismos de rate-limit para login: os decorators @ratelimit (ip 10/m, username 5/m) e o _check_rate_limit manual por IP (5/m) — alem do AxesMiddleware no settings. O _check_rate_limit incrementa o contador a CADA POST (inclusive logins bem-sucedidos), entao 5 logins validos seguidos do mesmo IP (recepcao com varias contas) bloqueiam o 6º com 'Muitas tentativas'. Logica duplicada e contador que conta sucessos = falso-positivo de bloqueio. except Exception: pass no cache tambem desabilita o limite silenciosamente.
  - **Fix:** Remover _check_rate_limit (os decorators @ratelimit + django-axes ja cobrem) OU, se mantido, incrementar o contador apenas em falha de autenticacao (apos authenticate retornar None), nao em todo POST. Logar quando o cache falhar em vez de pass silencioso.
- [ ] **[tests-coverage / lacuna (services sem teste direto)]** `aranha_estetica/services/` · gcal.py, push.py, lista_espera_service.py, agendamento_service.py, lgpd.py (export edge cases)
  - **Problema:** Varios services de negocio nao tem teste unitario dedicado: gcal.py (sync Google Calendar — falhas de API, idempotencia, dedup de eventos), push.py/webpush_views.py (web-push, subscription invalida/expirada), lista_espera_service.py (apenas o handler de notificacao em cancelamento e testado indiretamente; a logica de matching/expiracao do token nao). lgpd.py: o export DSAR e testado no caminho feliz, mas nao a completude/ausencia de PII vazada de OUTROS clientes no JSON exportado.
  - **Fix:** Priorizar lista_espera_service (matching de compatibilidade prof+proc+data, expiracao do token_reserva, nao notificar cliente bloqueado) e um assert no DSAR de que o export NAO contem dados de outro cliente. gcal/push podem ficar com testes de erro/idempotencia mockando a API externa.
- [ ] **[tests-coverage / qualidade (assert que nao testa nada)]** `aranha_estetica/tests/test_integration_booking.py` · test_agendamento_data_futura_obrigatoria (linhas 161-192)
  - **Problema:** O teste de 'data passada deve ser rejeitada' nao garante a rejeicao. O bloco de assert real esta dentro de 'if 'sucesso' not in resp.url:' (linha 181) — se a view ACEITAR a data passada (sucesso na URL), o teste pula a verificacao e so checa que nao houve 500. O proprio docstring admite 'the view has no explicit past-date guard'. Resultado: um booking com data passada poderia ser persistido e o teste passaria verde. Nao testa o invariante que promete.
  - **Fix:** Tornar o invariante incondicional: assertNotIn('sucesso', resp.url) E assertEqual(Atendimento.objects.filter(data_hora_inicio__lt=now).count(), 0) sempre. Se a view realmente nao tem guarda de data passada, isso e um bug de produto que o teste deveria expor (red), nao mascarar.
- [ ] **[tests-coverage / qualidade (nao exercita a logica real)]** `aranha_estetica/tests/test_integration_booking.py` · test_fluxo_completo_agendamento (linhas 95-114) e varios outros
  - **Problema:** O teste 'end-to-end' simula as transicoes de status por atribuicao direta (atd.status='CONFIRMADO'; atd.save() / atd.status='REALIZADO') e depois chama cliente.resetar_faltas() manualmente. Isso nao testa a FSM nem o caminho real de UI/admin que faz a transicao — testa apenas que o campo aceita o valor e que o metodo isolado funciona (ja coberto em test_cliente_faltas.py). O comentario 'Call resetar_faltas (the business logic...)' invocado a mao confirma que a integracao real (signal/FSM disparando o reset no REALIZADO) nao esta sendo verificada aqui.
  - **Fix:** Ou exercitar a transicao via o metodo de dominio (atd.marcar_realizado()/FSM) e verificar que o reset de faltas acontece como efeito colateral, ou remover a duplicacao e deixar test_cliente_faltas.py/test_signals.py cobrirem isso. Um 'integration test' deve disparar a cadeia real, nao recolocar os efeitos na mao.
- [ ] **[tests-coverage / qualidade (oraculo fraco em estado de bloqueio OTP)]** `aranha_estetica/tests/test_otp.py` · test_bloqueio_apos_max_tentativas (linhas 28-35)
  - **Problema:** O assert aceita dois motivos diferentes: assertIn(motivo, ('bloqueado','expirado')). Isso torna o teste tolerante a comportamentos divergentes — se um refactor trocar o caminho de bloqueio por 'expirado' (ou vice-versa), o teste continua verde, mascarando uma regressao na distincao entre codigo bloqueado por brute-force vs codigo expirado. Para um controle de seguranca (lockout), o motivo exato importa.
  - **Fix:** Separar em dois testes deterministas: um que verifica 'bloqueado' apos exatamente MAX_TENTATIVAS com codigo errado (sem consumir o codigo certo), e outro para 'expirado'. Evitar oraculo com OR em teste de seguranca.
- [ ] **[tests-coverage / risco sem teste (autorizacao admin CRUD)]** `aranha_estetica/tests/test_security.py` · StaffRequiredTests.ENDPOINTS_STAFF (linhas 28-37)
  - **Problema:** A matriz de authz cobre paineis read-only (overview, clientes, etc.) mas NAO cobre os endpoints de escrita/mutacao mais sensiveis: admin_usuarios (criar/editar usuario e papel — risco de escalonamento de privilegio/mass-assignment de papel=ADMIN), admin_financeiro, admin_management, gestao de profissionais (POST), reagendar/cancelar via admin. Sem grep retornando 'admin_usuarios'/'admin_management' nos testes. So o GET dos paineis e checado; os POST que mudam dados e papeis ficam sem teste de permissao.
  - **Fix:** Estender a matriz (ou criar test_admin_usuarios.py) para os endpoints POST de mutacao, incluindo um teste explicito de que um usuario nao-admin nao consegue se auto-promover a PAPEL_ADMIN via o form/view (mass-assignment), e que apenas superuser/admin cria usuarios.

## 🔵 BAIXA — nits (47)

- [ ] **[ARQUITETURA]** `aranha_estetica/models/agendamentos.py` · AtendimentoManager (linhas 48-60)
  - **Problema:** Manager re-declara manualmente proxies (ativos/futuros/hoje/...) que ja existem no QuerySet. Boilerplate duplicado e propenso a divergir (ex: 'passados' e 'conflito_com' existem no QS mas nem todos sao proxied de forma consistente; 'pendentes_aprovacao' aparece nos dois). Django ja oferece Manager.from_queryset(AtendimentoQuerySet) que expoe todos os metodos do QS automaticamente.
  - **Fix:** Substituir por objects = AtendimentoManager.from_queryset(AtendimentoQuerySet)() ou Manager.from_queryset(AtendimentoQuerySet)(), eliminando os proxies manuais.
- [ ] **[ARQUITETURA]** `aranha_estetica/views/booking_public.py` · modulo: WHATSAPP_NUMERO/CLINIC_NAME via os.environ.get com defaults hardcoded (linhas 39-40); duplicado em booking_reagendar.py:19 e agenda_publica usa SITE_URL
  - **Problema:** Numero de WhatsApp e nome da clinica lidos de os.environ com fallback hardcoded ('5517999990000', nome real da clinica) e duplicados entre modulos. Deveriam vir de settings para consistencia e evitar valores fantasma em producao sem env.
  - **Fix:** Centralizar em settings (ex settings.WHATSAPP_NUMERO, settings.CLINIC_NAME) e importar; remover defaults com dados reais.
- [ ] **[ARQUITETURA]** `aranha_estetica/services/notificacao.py` · EmailService.enviar_confirmacao_compra_pacote (l.114-125)
  - **Problema:** Acoplamento a helper privado e template trocado: chama _enviar_email (funcao privada de utils.email, prefixo _) diretamente e reaproveita o template 'email/pacote_expirando.html' para uma confirmacao de COMPRA (assunto diz compra, corpo e de expiracao). Confunde semantica e fura o encapsulamento da camada utils.email (que expoe funcoes publicas para os outros envios).
  - **Fix:** Criar uma funcao publica enviar_confirmacao_compra_pacote_email em utils.email com template proprio e chama-la, em vez de usar _enviar_email + template de expiracao.
- [ ] **[ARQUITETURA]** `aranha_estetica/tasks.py` · varios jobs (l.35-36, 79, 130-131, 201, 239-240, 289-290, 363-364, 407, 460)
  - **Problema:** Imports feitos dentro do corpo de cada task (lazy import repetido) para evitar import circular. Funciona, mas espalha o mesmo padrao por todas as tasks, dificulta leitura e esconde dependencias. Indica acoplamento entre tasks e utils/models que poderia ser resolvido na estrutura de modulos.
  - **Fix:** Avaliar mover imports para o topo onde nao houver ciclo real; onde houver, centralizar a justificativa. Reduz boilerplate e clarifica dependencias.
- [ ] **[ARQUITETURA / PADRAO (import por __import__ e import local)]** `aranha_estetica/views/notificacoes.py` · admin_cancelar_agendamento, linha 131 (token=__import__('secrets').token_urlsafe(32))
  - **Problema:** Uso de __import__('secrets') inline em vez de import normal no topo — antipadrao que dificulta leitura e analise estatica. A criacao da Notificacao de CANCELAMENTO so ocorre se cliente.email existir (dentro do if), entao cancelamentos sem e-mail nao geram registro de Notificacao (inconsistencia no historico). Tambem ha read-modify-write de atendimento.status sem transacao envolvendo o e-mail/Notificacao.
  - **Fix:** Mover `import secrets` para o topo. Decidir se a Notificacao deve ser criada independente de haver email (para auditoria/historico). Considerar transaction.atomic envolvendo save do atendimento + criacao da Notificacao.
- [ ] **[CORRECAO]** `aranha_estetica/models/pacotes.py` · CompraPacote.save (linhas 78-85)
  - **Problema:** data_expiracao usa timezone.now() + relativedelta(...).date(). timezone.now() e UTC; aplicar .date() apos somar meses pode resultar em data de expiracao deslocada em 1 dia perto da meia-noite local (ex: criado 21:30 BRT = 00:30 UTC do dia seguinte). Para validade de pacote (12 meses) o impacto e pequeno, mas e um off-by-one de fuso real.
  - **Fix:** Usar timezone.localdate() como base: (timezone.localdate() + relativedelta(months=...)).
- [ ] **[CORRECAO]** `aranha_estetica/models/procedimentos.py` · Procedimento.save slug generation (linhas 83-93) e Promocao.esta_vigente (178-182)
  - **Problema:** Geracao de slug faz loop com .exists() por tentativa (N queries em colisao) e nao e atomica vs unique=True (mesma classe de race do codigo_indicacao do Cliente). Promocao.esta_vigente e property que usa timezone.now().date() — ok, mas import dentro da property (linha 180) e nit; alem disso esta_vigente nao considera o XOR desconto/preco, apenas datas+ativa (semanticamente ok mas nome sugere prontidao total).
  - **Fix:** Aceitar e tratar IntegrityError no save do slug (retry com sufixo), mover import de timezone para o topo. Opcional: renomear/documentar esta_vigente.
- [ ] **[CORRECAO]** `aranha_estetica/views/public.py` · lista_espera_publica, linha 297
  - **Problema:** data_obj < timezone.now().date() rejeita datas passadas mas ACEITA hoje. Combinado com o turno opcional, permite inscricao para 'hoje' mesmo que ja seja fim do dia. Edge case menor; confirmar_agendamento usa idade>=18 e feriado mas aqui nao ha checagem de turno vs hora atual.
  - **Fix:** Se a intencao e somente datas futuras, usar `<=`; caso hoje seja valido, documentar. Avaliar regra de turno ja decorrido.
- [ ] **[CORRECAO]** `aranha_estetica/services/retorno_service.py` · sugerir_retorno (l.52-58)
  - **Problema:** Calculo da data sugerida do retorno pode cair no passado/horario invalido. base = data_hora_fim do atendimento origem; se min_dias=0 e max_dias=0 (defaults quando proc.retorno_minimo_dias e _maximo_dias sao None/0, l.53-54), a janela colapsa e sugerida = data_hora_fim (mesmo instante do fim do atendimento), gerando retorno no mesmo horario — frequentemente colidindo com o proprio atendimento ou sem sentido de agenda. Nao ha validacao de horario comercial nem de futuro.
  - **Fix:** Definir um minimo sensato (ex.: pelo menos +1 dia) quando retorno_minimo_dias nao configurado, e/ou normalizar a sugestao para um horario comercial valido em vez de herdar o instante de fim.
- [ ] **[CORRECAO]** `aranha_estetica/tasks.py` · job_alerta_detrator_nps linha 138-139
  - **Problema:** if not detratores.exists(): return — depois o queryset e iterado novamente (l.146 e l.158), reavaliando a query. .exists() seguido de iteracao da mesma queryset executa 2+ queries. Minor, mas a queryset detratores e avaliada multiplas vezes (exists + loop sem email + loop com email).
  - **Fix:** Materializar uma vez (lista = list(detratores)) e usar len()/checagem in-memory, ou aceitar o custo. Nao critico.
- [ ] **[CORRECAO]** `aranha_estetica/middleware.py` · Enforce2FAMiddleware.__call__ (linha 39)
  - **Problema:** O gating 2FA cobre apenas paths que comecam com '/painel/' ou '/profissional/' por string. Novas rotas administrativas fora desses prefixos (ou montadas em outra raiz) nao seriam protegidas pelo desafio 2FA. Acoplamento fragil a prefixo de URL hardcoded.
  - **Fix:** Considerar gatear por permissao (request.user.is_staff / vinculo profissional) ou centralizar a lista de prefixos protegidos numa constante unica testada, evitando rota nova ficar sem 2FA por engano.
- [ ] **[CORRECAO]** `C:/Users/rafae/Documents/shivazen-app/manage.py` · L14 os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'clinica.settings')
  - **Problema:** manage.py (L14), wsgi.py (L14), asgi.py (L14) e celery.py (L5) usam setdefault para 'clinica.settings', que resolve via __init__.py com fallback dev. Para comandos administrativos rodados em prod (ex: migrate no release/startCommand), se DJANGO_ENV/RAILWAY_ENVIRONMENT_NAME nao estiver presente no ambiente do comando, o migrate roda sob settings de DEV (SQLite!), nao tocando o Postgres de prod. Reforca o risco do achado #1.
  - **Fix:** Garantir DJANGO_ENV=prod no ambiente de todos os processos (web/worker/beat/release). Idealmente o __init__.py deveria defaultar para prod (fail-safe), nao dev.
- [ ] **[CORRECAO (timezone/localdate em janela de semana)]** `aranha_estetica/views/profissional.py` · agenda, linhas 49-76
  - **Problema:** filtragem usa data_hora_inicio__date=dia e __date__gte/__lt. O lookup __date converte para a timezone do banco/sessao; se USE_TZ=True e o banco em UTC, atendimentos perto da meia-noite local podem cair no dia errado dependendo da config de timezone do connection. agenda_por_dia[at.data_hora_inicio.date()] usa .date() do datetime aware (em UTC se nao convertido), podendo gerar KeyError se a data UTC sair fora do intervalo dias_semana (que usa localdate).
  - **Fix:** Usar timezone.localtime(at.data_hora_inicio).date() ao indexar agenda_por_dia, ou .get(...) com fallback, garantindo consistencia com timezone.localdate(). Confirmar TIME_ZONE/USE_TZ e que o __date lookup respeita a tz local.
- [ ] **[DADOS/QUERY]** `aranha_estetica/models/clientes.py` · Cliente.all_objects = models.Manager() (linha 78)
  - **Problema:** all_objects e um Manager nu (retorna TODOS, incluindo soft-deleted) mas se chama 'all_objects' enquanto o default 'objects' filtra deletados. _gerar_codigo_indicacao usa Cliente.all_objects.filter(...) corretamente. Porem por convencao no resto do codebase o mixin chama de all_objects um SoftDeleteManager-derivado; aqui e Manager() puro. Inconsistencia sutil de semantica entre Cliente e os models que usam SoftDeleteMixin.
  - **Fix:** Padronizar nomenclatura/semantica de managers de soft-delete entre Cliente e SoftDeleteMixin (idealmente migrar Cliente para o mixin, ver finding alta).
- [ ] **[DADOS/QUERY]** `aranha_estetica/models/agendamentos.py` · Notificacao.token (linha 293) e ListaEspera.token_reserva (sistema.py linha 27)
  - **Problema:** Notificacao.token tem unique=True mas blank/null=True e nenhum default/geracao automatica no save (diferente de Atendimento.token_cancelamento que e auto-gerado). Risco de varias linhas com token NULL (ok em unique) mas a geracao fica espalhada nas views/services, sem garantia de unicidade centralizada. ListaEspera.token_reserva nem unique tem.
  - **Fix:** Centralizar geracao de token via default=callable ou save() como feito em Atendimento, e decidir se token_reserva precisa de unique.
- [ ] **[DADOS/QUERY]** `aranha_estetica/views/dashboard.py` · painel_overview, pendentes_aprovacao linhas 96-103
  - **Problema:** Faz duas queries com filtro identico (status='PENDENTE', data futura): uma para a lista [:15] e outra .count() para total_pendentes. Sao duas roundtrips para o mesmo conjunto base.
  - **Fix:** Se a contagem total importa, manter o count separado e ok; mas considerar reaproveitar a contagem ou usar uma unica agregacao. Nit de eficiencia, baixa prioridade.
- [ ] **[DADOS/QUERY]** `C:/Users/rafae/Documents/shivazen-app/clinica/settings/base.py` · SESSION_ENGINE db L413 (sem Redis)
  - **Problema:** Sem REDIS_URL, sessoes vao para o DB (django.contrib.sessions.backends.db). Combinado com SESSION_SAVE_EVERY_REQUEST=False (bom) ainda gera escrita/leitura no Postgres por sessao autenticada. Sem Redis em prod, isso adiciona carga ao DB. Nao e bug, mas e perf em prod sem Redis (que e o cenario padrao do .env.example com REDIS_URL vazio).
  - **Fix:** Recomendar Redis em prod (ja parcialmente previsto). Se DB sessions for mantido, garantir limpeza periodica de sessoes expiradas (clearsessions) — nao ha job no CELERY_BEAT_SCHEDULE para isso.
- [ ] **[DADOS/QUERY (.count() em vez de exists / contagem dupla)]** `aranha_estetica/views/notificacoes.py` · painel_notificacoes, linhas 79-86
  - **Problema:** Executa notifs.count() + 3 filtros .count() (confirmados/cancelados/sem_resposta) = 4 queries de agregacao sobre o mesmo queryset, depois pagina (mais 1-2 queries). Pode ser consolidado numa unica agregacao condicional.
  - **Fix:** Usar uma agregacao: notifs.aggregate(total=Count('id'), confirmados=Count('id', filter=Q(resposta='CONFIRMOU')), cancelados=Count('id', filter=Q(resposta='CANCELOU')), sem_resposta=Count('id', filter=Q(resposta__isnull=True))) numa unica passada.
- [ ] **[PADRAO]** `aranha_estetica/models/sistema.py` · CodigoOtp (linhas 149-152, 191-285)
  - **Problema:** import os as _os no corpo da classe (linha 149) e varios imports locais dentro de metodos (hashlib, secrets, timedelta, timezone repetidos em gerar/verificar/pode_reenviar). Configuracao de TTL/MAX_TENTATIVAS lida de env em tempo de import de classe (linha 150-152) — congela o valor no carregamento do modulo, dificulta override em testes via override_settings. Logica de negocio (gerar/verificar OTP) vive em classmethods do model em vez de service — model com bastante responsabilidade de fluxo.
  - **Fix:** Mover imports para o topo do modulo; ler TTL/MAX via settings (settings.OTP_TTL) em vez de os.environ no import; considerar extrair gerar/verificar para um service de OTP. (CHECK chk_otp_tentativas_teto<=10 mas MAX_TENTATIVAS default=5 — teto do banco maior que limite logico, ok mas documentar.)
- [ ] **[PADRAO]** `aranha_estetica/models/prontuario.py` · Prontuario (linhas 7-27)
  - **Problema:** respostas_extras (JSONB) substituiu EAV e a busca depende de filtros JSON (respostas_extras__diabetes=True) mas nao ha GinIndex declarado sobre o JSONField. Consultas de filtro por chave em JSONB ficam sem indice (seq scan) conforme a base cresce. Tambem nao ha __str__ no model.
  - **Fix:** Adicionar django.contrib.postgres.indexes.GinIndex(fields=['respostas_extras']) se houver queries por chave; adicionar __str__ (ex: f'Prontuario {self.cliente_id}').
- [ ] **[PADRAO]** `aranha_estetica/models/anamnese.py` · RespostaAnamnese.__str__ / FormularioAnamnese clean
  - **Problema:** RespostaAnamnese.__str__ acessa self.formulario.nome e self.cliente.nome (2 queries lazy se nao prefetched) — pode causar N+1 em listagens de admin. FormularioAnamnese.clean valida schema_json mas full_clean nao roda automaticamente em save() — se a criacao do formulario nao chamar full_clean (ex: via API/fixture), schema invalido entra no banco sem validacao.
  - **Fix:** No __str__ usar self.cliente_id/formulario_id ou garantir select_related nas listagens; garantir que fluxos de escrita de FormularioAnamnese chamem full_clean() (ou validar no serializer/form).
- [ ] **[PADRAO]** `aranha_estetica/views/booking_api.py` · cancelar_agendamento linha 297; logger.error(f'...{e}')
  - **Problema:** Uso de f-string interpolando a excecao na mensagem de log (em vez de logging lazy/extra), e mensagens/keys de JSON inconsistentes ('erro' aqui vs 'error' nos demais endpoints do mesmo arquivo), dificultando o consumo no front e padronizacao.
  - **Fix:** Padronizar a chave de erro ('error') em todo booking_api.py e usar logger.error('cancelar_agendamento_falha', exc_info=True) sem interpolar a excecao no template da mensagem.
- [ ] **[PADRAO]** `aranha_estetica/views/admin_management.py` · termo_assinatura, linhas 403-409
  - **Problema:** Bloco duplicado: assinados_ids.update(AceiteTermo.objects.filter(cliente=cliente).values_list('versao_termo_id', flat=True)) aparece duas vezes identico (copy-paste), executando a mesma query duas vezes sem efeito util.
  - **Fix:** Remover a segunda chamada redundante.
- [ ] **[PADRAO]** `aranha_estetica/views/admin_financeiro.py` · dashboard_financeiro, cache_key linha 34
  - **Problema:** cache_key inclui apenas a data (hoje.isoformat()) com TTL de 60s. Os dados 'hoje/semana/mes' usam timezone.now() (agora) mas o resultado e reaproveitado por todos os usuarios durante a janela do TTL — aceitavel, porem a chave por data dá falsa impressao de granularidade diaria quando o TTL real e de 60s. Sem bug, apenas naming/intencao confusa.
  - **Fix:** Documentar que o cache e por instancia de 60s (a data so evita colisao trivial entre dias) ou simplificar a chave.
- [ ] **[PADRAO]** `aranha_estetica/views/admin_2fa.py` · admin_2fa_setup, linha 66
  - **Problema:** secret_b32 e derivado e exibido manualmente (base32 do device.key). O config_url do device ja contem o segredo no QR; expor tambem o segredo em texto e redundante e amplia a exposicao do shared secret na resposta HTML.
  - **Fix:** Avaliar se a exibicao textual do segredo e realmente necessaria; se for (fallback de digitacao manual), garantir que so apareca durante o setup pendente (ja e o caso) e nunca seja logada.
- [ ] **[PADRAO]** `aranha_estetica/services/agendamento.py` · criar (l.34) e agendamento_service.py:117, _slot_ocupado:254
  - **Problema:** Constante magica e import local repetido: fallback de duracao 30 minutos hardcoded em 3 lugares (agendamento.py:34 `procedimento.duracao_minutos or 30`, retorno_service.py:58 `or 30`). Alem disso `from datetime import timedelta` aparece inline dentro de metodos (agendamento_service.py:116, 253) em vez do topo, ja importado em outros arquivos.
  - **Fix:** Extrair DURACAO_PADRAO_MINUTOS para constants.py e reusar; mover imports de timedelta para o topo do modulo.
- [ ] **[PADRAO]** `aranha_estetica/services/notificacao.py` · class NotificacaoService (l.57-62)
  - **Problema:** Atribuicao de staticmethods da base como atributos de classe (registrar = _BaseNotificacao._registrar, etc.). Funciona mas e fragil/obscuro: NotificacaoService.registrar(...) chama o staticmethod da base sem heranca de comportamento e confunde leitura. Comentario diz 'API legada' — dead-ish code mantido por compat.
  - **Fix:** Se ainda usado, herdar normalmente e expor metodos via heranca; se nao usado, remover a classe legada. Verificar grep de NotificacaoService.registrar/marcar_* antes.
- [ ] **[PADRAO]** `aranha_estetica/utils/whatsapp.py` · imports 'from .precos import mask_telefone' repetidos (linhas 65, 90, 137, 167)
  - **Problema:** mask_telefone e importado localmente dentro das funcoes quatro vezes, importando de precos.py (um modulo de precos) so para mascarar telefone. Acoplamento confuso de responsabilidades (utilitario de PII morando em modulo de preco) e repeticao desnecessaria do import.
  - **Fix:** Importar uma unica vez no topo do modulo a partir do modulo canonico de PII (apos consolidacao do item de mask_*). Mover a funcao para pii.py.
- [ ] **[PADRAO]** `aranha_estetica/utils/sms.py` · pode_enviar/enviar_sms/enviar_otp_sms
  - **Problema:** Uso de 'ip: str = None' (deveria ser Optional[str]) e ausencia de comentario sobre o tratamento do ZENVIA_API_TOKEN. Constante 3600 (TTL de 1h) repetida em magic number em 3 pontos. _tentativa como parametro publico-aparente (prefixo _ ajuda mas e parametro posicional recursivo) e fragil.
  - **Fix:** Usar Optional[str]; extrair RATE_LIMIT_TTL = 3600 como constante; documentar que ZENVIA_API_TOKEN vem so de env (ok, nao hardcoded). Considerar loop em vez de recursao para os retries.
- [ ] **[PADRAO]** `aranha_estetica/tasks.py` · job_promocao_mensal (l.353), send_email_async (l.454), job_notificar_fila_espera (l.198)
  - **Problema:** Falta de type hints nas assinaturas das tasks (assunto, corpo_html_partial, procedimento_id, etc.) e mistura de logging f-string (logger.info(f'...')) com logging estruturado (extra={}). Inconsistencia de estilo e perda de campos estruturados em metade dos logs.
  - **Fix:** Adicionar type hints e padronizar logging estruturado (sempre extra={}), evitando f-strings em logger para permitir agregacao/parse.
- [ ] **[PADRAO]** `aranha_estetica/constants.py` · DESCONTO_ANIVERSARIO_PERCENTUAL (linha 50) e TAXA_NO_SHOW_PERCENTUAL (linha 41)
  - **Problema:** Inconsistencia de tipo/unidade entre constantes financeiras: TAXA_NO_SHOW_PERCENTUAL = Decimal('0.50') (fracao), mas DESCONTO_ANIVERSARIO_PERCENTUAL = 15 (int, percentual inteiro). Misturar fracao Decimal e percentual int convida a bug de calculo (multiplicar por 15 em vez de 0.15) e perda de precisao se usado em aritmetica monetaria.
  - **Fix:** Padronizar representacao (sempre Decimal fracao, ex: Decimal('0.15')) e nomear claramente unidade (_FRACAO vs _PCT). Documentar a convencao no topo do arquivo.
- [ ] **[PADRAO]** `aranha_estetica/forms/agendamento.py` · AgendamentoPublicoForm.datetime (linha 28) e clean_datetime (linha 43)
  - **Problema:** Campo chamado 'datetime' faz shadow do nome do modulo datetime importado no topo (linha 2). Funciona (e atributo de classe), mas e armadilha de legibilidade. Alem disso clean_datetime retorna um datetime naive (sem tzinfo) — se for usado direto para gravar/comparar com USE_TZ=True gera warning/erro de naive datetime.
  - **Fix:** Renomear o campo para 'data_hora' (mantendo mapeamento no template) e tornar o datetime aware via django.utils.timezone.make_aware no clean_datetime ou na camada de service.
- [ ] **[PADRAO]** `aranha_estetica/decorators.py` · modulo inteiro
  - **Problema:** Ausencia de type hints e imports fora da ordem PEP8 (functools.wraps importado por ultimo, apos imports do django). Diferente do restante do modulo cross-cutting que ja usa type hints (validators.py).
  - **Fix:** Reordenar imports (stdlib functools antes de django) e adicionar anotacoes basicas. Nit de consistencia.
- [ ] **[PADRAO]** `C:/Users/rafae/Documents/shivazen-app/clinica/settings/base.py` · SECURE_PROXY_SSL_HEADER L122 (base) e prod.py L20
  - **Problema:** SECURE_PROXY_SSL_HEADER e definido em base.py L122 e redefinido identico em prod.py L20 — duplicacao. Em dev (atras de nenhum proxy) o header em base.py faz o Django confiar em X-Forwarded-Proto: https mesmo localmente; nao e exploravel em dev local, mas e codigo morto/confuso ter em ambos.
  - **Fix:** Mover SECURE_PROXY_SSL_HEADER e USE_X_FORWARDED_HOST apenas para prod.py (onde ha proxy real Railway/Cloudflare) e remover de base.py, evitando que dev confie cegamente em headers forjados.
- [ ] **[PADRAO]** `C:/Users/rafae/Documents/shivazen-app/clinica/settings/base.py` · LOGGING formatters 'json' L360-366
  - **Problema:** O dict do formatter 'json' usa o operador ternario inline (... if not DEBUG else {...}) dentro do dict literal — quando DEBUG=True o formatter chamado 'json' vira na verdade um formatter de texto. Como _LOG_FORMATTER so vira 'json' quando not DEBUG (L344-350), em dev o formatter 'json' nunca e referenciado, entao o ramo else e codigo morto. Adiciona complexidade sem efeito.
  - **Fix:** Remover o ramo else do formatter 'json' (definir 'json' apenas quando not DEBUG, ou sempre como JsonFormatter). Simplifica e remove dead code.
- [ ] **[PADRAO (duplicacao de codigo)]** `aranha_estetica/views/profissional.py` · aprovar_agendamento (169-186) vs rejeitar_agendamento (191-221)
  - **Problema:** Os dois handlers sao quase identicos: mesmo guard de status, mesmo bloco de montagem de 'dados' e mesmo padrao send_email_async.delay(...) com try/except fallback sincrono. O except Exception ao redor do .delay() engole qualquer erro e chama a versao sincrona — mascarando, por exemplo, um erro de serializacao do payload (nao so Celery indisponivel).
  - **Fix:** Extrair um helper _notificar_status_async(nome_task, email, dados) e um _mudar_status_pendente(atendimento, novo_status, acao_log). Restringir o except do .delay() a falhas de broker (OperationalError/ConnectionError) em vez de Exception amplo, para nao mascarar bugs de payload.
- [ ] **[PADRAO (duplicacao de util / import inconsistente)]** `aranha_estetica/views/whatsapp.py` · linha 15 (from ..utils.precos import mask_telefone)
  - **Problema:** Existem TRES implementacoes de mask_telefone no codebase (utils/precos.py, utils/security.py, utils/pii.py) com assinaturas/comportamentos ligeiramente diferentes. whatsapp.py importa de precos, serializers.py de security. Risco de mascaramento inconsistente de PII em logs vs respostas e manutencao duplicada.
  - **Fix:** Consolidar mask_telefone/mask_cpf/mask_email em um unico modulo (utils/pii.py) e reexportar dos demais, ou remover duplicatas e ajustar imports.
- [ ] **[PADRAO (type hints / docstrings / dead var)]** `aranha_estetica/views/prontuario.py` · prontuario_detalhe (98-104) variaveis aceites/assinaturas; _perguntas_configuradas usa logger antes de definido (37 vs 40)
  - **Problema:** (1) aceites e assinaturas consultam praticamente a mesma tabela AceiteTermo do mesmo cliente com select_related diferente — provavel redundancia (uma das duas e dead/duplicada no template). (2) _perguntas_configuradas (linha 37) referencia `logger` que so e definido na linha 40 (depois da funcao); funciona porque so e avaliado em runtime, mas e fragil e confuso ordenar o logger apos uma funcao que o usa.
  - **Fix:** Definir logger no topo do modulo (antes de qualquer funcao). Verificar no template se aceites e assinaturas sao ambos usados; se nao, remover a query redundante (economiza 1 query + select_related).
- [ ] **[SEGURANCA]** `aranha_estetica/views/booking_api.py` · verificar_telefone, linha 211 (json.loads(request.body))
  - **Problema:** json.loads(request.body) sem try/except: corpo JSON malformado com content_type application/json levanta JSONDecodeError -> 500 (nao tratado nesta view, ao contrario de cancelar_agendamento que captura). Endpoint publico, facilmente fuzzavel.
  - **Fix:** Envolver o parse em try/except json.JSONDecodeError retornando 400, como ja feito em cancelar_agendamento (linha 294).
- [ ] **[SEGURANCA]** `aranha_estetica/services/otp.py` · solicitar_otp/log (l.65, 82)
  - **Problema:** Uso de hash(email) builtin do Python como 'email_hash' em log (l.65 e 82). hash() de str e randomizado por processo (PYTHONHASHSEED) — nao e estavel entre execucoes/processos, entao nao serve para correlacionar logs do mesmo email; e nao e criptografico (poderia colidir/ser revertido em dominios pequenos). Da falsa sensacao de anonimizacao util.
  - **Fix:** Se o objetivo e correlacao sem PII, usar hashlib.sha256(email.encode()).hexdigest()[:12] (estavel) ou apenas omitir; documentar que nao e seguranca, e correlacao.
- [ ] **[SEGURANCA]** `aranha_estetica/utils/pii.py` · sentry_before_send (linhas 60-82)
  - **Problema:** O scrub de PII em Sentry so cobre event['extra'] e request['data']. PII pode vazar por outros caminhos do payload Sentry: breadcrumbs, exception values/messages (ex.: ValidationError citando email/CPF), event['request']['query_string'] e headers (Cookie/Authorization). A docstring menciona contexts.custom mas o codigo nao percorre contexts.
  - **Fix:** Estender o scrub para breadcrumbs, exception.values[].value, query_string e headers sensiveis; ou habilitar send_default_pii=False + max scrubbing nativo do SDK. Implementar o tratamento de contexts mencionado na docstring (ou remover a mencao).
- [ ] **[SEGURANCA]** `aranha_estetica/tasks.py` · job_alerta_detrator_nps linhas 146-153 e 160-167
  - **Problema:** Leak de PII em logs: logger.warning('nps_detrator_sem_email_admin_cliente', extra={'cliente': avaliacao.atendimento.cliente.nome, ...}) e o corpo do email monta nome/procedimento/profissional. Nome do cliente associado a nota baixa de NPS e dado pessoal+sensivel de relacionamento; logar nome em nivel warning expoe PII nos logs de aplicacao.
  - **Fix:** Logar cliente_id em vez de nome nos logs estruturados; manter nome apenas no corpo do email destinado ao admin. Revisar nivel (info/debug) e retencao de logs.
- [ ] **[SEGURANCA]** `aranha_estetica/forms/cliente.py` · ClienteForm.Meta (linha 8) — campo telefone/cpf
  - **Problema:** ClienteForm (ModelForm) inclui cpf e telefone mas nao aplica os validators de dominio (validate_cpf, validate_telefone_br) no nivel de form — depende inteiramente de o model chama-los. Se o model nao tiver esses validators no field, CPF/telefone invalidos passam. (Verificar no model; se ja validado no model.save, ok, mas form bound nao reaproveita a mensagem amigavel.)
  - **Fix:** Confirmar que Cliente.cpf/telefone tem validators=[validate_cpf/validate_telefone_br]; caso contrario adicionar clean_cpf/clean_telefone no form para feedback de validacao consistente.
- [ ] **[SEGURANCA]** `C:/Users/rafae/Documents/shivazen-app/clinica/urls.py` · path('django-admin-sv/', admin.site.urls) L20
  - **Problema:** O admin esta em path obscurecido ('django-admin-sv/') e protegido por AdminSiteOTPRequired (comentario L12) — bom. Porem nao ha defesa em profundidade adicional (ex: IP allowlist) e o caminho ainda e descobrivel. Baixa severidade pois OTP e exigido.
  - **Fix:** Opcional: restringir o admin por IP/VPN em prod via middleware ou config do proxy, e confirmar que AdminSiteOTPRequired esta de fato aplicado em aranha_estetica.apps.ready() (referenciado mas fora deste escopo).
- [ ] **[tests-coverage / qualidade (acoplamento a detalhe de impl via monkeypatch de modulo)]** `aranha_estetica/tests/test_whatsapp_webhook.py` · linhas 45-46, 55-56, 68-69, 82-83, 99-100
  - **Problema:** Os testes setam o secret mutando atributo de modulo em runtime (whatsapp_mod.WHATSAPP_APP_SECRET = APP_SECRET) sem restaurar o valor original. Isso vaza estado entre testes (a ordem de execucao passa a importar) e acopla o teste a uma variavel de modulo em vez de a settings/override_settings. test_webhook_fail_closed seta para '' mas nao garante restauracao para os testes seguintes.
  - **Fix:** Mover o secret para settings e usar @override_settings(WHATSAPP_APP_SECRET=...) (com leitura via getattr(settings,...) na view), ou usar unittest.mock.patch.object como context manager para restaurar automaticamente. Elimina vazamento de estado entre testes.
- [ ] **[tests-coverage / qualidade (factory com kwargs ignorado / parametro morto)]** `aranha_estetica/tests/factories.py` · criar_procedimento (linhas 34-48) parametro categoria/duracao defaults e _seq_telefone (linha 51)
  - **Problema:** _seq_telefone = iter(range(100000)) e um iterador de modulo: se a suite gerar >100k clientes acumulados (improvavel, mas) StopIteration estoura; e por ser estado global de modulo, o numero de telefone depende da ordem/quantidade de testes ja executados, dificultando reproducao de um teste isolado que assuma um telefone especifico. Menor: nao ha factory para Usuario/Promocao/RegraComissao (cada teste reescreve create_user inline, ver _criar_staff em test_security.py e create_superuser em test_gaps_coverage.py), gerando duplicacao.
  - **Fix:** Trocar o iterador por um contador via itertools.count() (sem limite) ou uuid no telefone; e centralizar criar_staff/criar_usuario_comum/criar_promocao em factories.py para remover a duplicacao espalhada em test_security.py, test_decorators.py e test_gaps_coverage.py.
- [ ] **[tests-coverage / qualidade (import dentro do metodo de teste)]** `aranha_estetica/tests/test_security.py` · AdminAtualizarStatusTests — 'import json' repetido nas linhas 117,126,137,151
  - **Problema:** import json esta repetido dentro de cada metodo de teste em vez de no topo do modulo. Ruido/PEP8, dificulta leitura e e duplicacao desnecessaria. Mesmo padrao de imports tardios (from django.contrib import admin / from two_factor.admin import ...) dentro de funcoes aparece em test_2fa.py.
  - **Fix:** Mover 'import json' (e os imports tardios obvios) para o topo do modulo. Imports locais so se justificam para quebrar ciclo ou import caro/condicional.
